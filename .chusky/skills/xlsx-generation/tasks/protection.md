# Protection and locked cells

If users will edit only designated inputs:
- visually distinguish input cells;
- unlock only intended inputs;
- protect calculated/output regions if supported.

Do not present workbook protection as strong security. It is an editing control, not a substitute for encryption or access control.
