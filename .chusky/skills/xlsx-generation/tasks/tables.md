# Excel tables

Use a table when data is row-oriented and expected to grow.

Rules:
- include a single header row;
- use a unique table name;
- keep headers short and meaningful;
- avoid blank columns inside the table;
- keep computed columns formula-driven;
- use totals rows only when useful.

Tables are preferable to hand-painted ranges for trackers because filters and structured growth are built in.
