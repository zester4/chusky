# Hyperlinks and sources

For researched data:
- include plain source URLs in a dedicated `Source` column when row-wise;
- for model assumptions, source URLs can be stored in comments/notes or adjacent cells.

Do not store authenticated or secret-bearing URLs.
