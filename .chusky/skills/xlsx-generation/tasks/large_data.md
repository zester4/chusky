# Large datasets

For large data:
- write blocks;
- avoid per-cell calls;
- consider CSV import;
- avoid formatting every cell individually;
- keep formulas limited to necessary columns;
- use tables/filters;
- summarize on a separate sheet.

Do not render millions of cells. Render the dashboard/header/sample region instead.
