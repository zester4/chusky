# Conditional formatting

Use it to surface:
- overdue dates;
- negative values;
- threshold breaches;
- completion status;
- outliers;
- progress bars.

Prefer a few meaningful rules. Avoid a rainbow of redundant rules.

When formulas drive a rule, confirm the relative reference is anchored to the top-left cell of the target range.
