# Specialist delegation handoff

When Chusky delegates to Nora, Lucas, Maya, Leo, Sofia, Dexter, Elena, Ivy, Quinn, Aria, Kai:

- Objective scoped to that worker's domain
- Allowed tools already enforced by contract — do not ask them to exceed it
- Expected output format stated (brief, patch, digest, script)
- Prior specialist output summarized if multi-stage

On return:

- Verify output against expectedOutput
- Integrate or re-delegate; do not rubber-stamp
