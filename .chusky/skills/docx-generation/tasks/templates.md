# Templates and style packs

When a reference DOCX or DOTX is supplied:
- inspect its named styles;
- reuse its section/page settings where appropriate;
- preserve theme fonts/colors if they matter;
- avoid copying unrelated content;
- verify every reused style renders correctly.

If no template exists, use a restrained professional style system rather than inventing elaborate decoration.
