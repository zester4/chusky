/**
 * Example route usage — adapt to your TanStack Start file-route conventions.
 * Place GameCanvas under src/routes (or equivalent) and link from a nav entry.
 *
 * import { GameCanvas } from './GameCanvas'
 *
 * export const Route = createFileRoute('/game/demo')({
 *   component: () => (
 *     <main style={{ minHeight: '100vh', display: 'grid', placeItems: 'center', background: '#0b1220', color: '#e8eef7' }}>
 *       <GameCanvas />
 *     </main>
 *   ),
 * })
 */

export {}
