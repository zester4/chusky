const baseChuckTools = [
  { type: "function", function: { name: "CHUCK_SEARCH_SKILLS", description: "Search the trusted project skill catalogue for a relevant workflow. This is read-only and searches .chusky/skills metadata and bounded instruction excerpts; it never reads arbitrary filesystem paths or grants tools.", parameters: { type: "object", properties: { query: { type: "string", description: "Task or capability to match, such as create a professional Excel workbook" }, limit: { type: "number", minimum: 1, maximum: 20 } }, required: ["query"] } } },
  { type: "function", function: { name: "CHUCK_LIST_SKILL_FILES", description: "List the files bundled inside one trusted project skill, including nested references, scripts, and assets. Paths are returned relative to that skill and cannot escape its directory.", parameters: { type: "object", properties: { name: { type: "string", description: "Skill name" }, maxFiles: { type: "number", minimum: 1, maximum: 200 } }, required: ["name"] } } },
  { type: "function", function: { name: "CHUCK_READ_SKILL_FILE", description: "Read one text file from a named trusted project skill when its SKILL.md references supporting material. Use a relative path such as SKILL.md or references/formatting.md. Binary assets return metadata only; this tool never executes scripts or reads outside the skill directory.", parameters: { type: "object", properties: { name: { type: "string", description: "Skill name" }, path: { type: "string", description: "Relative path inside the skill; defaults to SKILL.md" }, maxChars: { type: "number", minimum: 1, maximum: 262144 } }, required: ["name"] } } },
  { type: "function", function: { name: "CHUCK_CREATE_PDF", description: "Create and deliver a polished PDF in Daytona with structured text, bullets, tables, images, charts, page breaks, a letterhead, color presets, safe font themes, headers, footers, and page numbers. Use workspace-relative image paths. The generator uses ReportLab with wrapping, proportional image sizing, split/repeating-header tables, alternating row styling, numeric-cell alignment, and a real PDF reopen check; the resulting PDF is structurally validated and rendered page-by-page before delivery. Use the shared brand object whenever company identity matters; its logo appears at the top right of every page. For long playbooks, provide substantive structured sections rather than a short outline; this tool does not invent operational detail or page count. Use this instead of asking the model to hand-write raw PDF bytes.", parameters: { type: "object", properties: { title: { type: "string" }, sections: { type: "array", items: { type: "object" }, description: "Sections may contain heading, body, bullets, table as a matrix or {headers,rows}, optional columnWidths as relative weights such as [1,3,1], imagePath, imageAltText, imageWidth, chart {categories,series}, and pageBreakBefore." }, brand: { type: "object", description: "Reusable company identity for every artifact: companyName, tagline, workspace-relative logoPath, header/footer, preset executive|modern|bold|minimal|brand, fontFamily sans|serif|mono, and six-digit hex colors primary/accent/secondary/background/surface/text/muted. Explicit style values override brand values." }, style: { type: "object", description: "Optional pageSize A4|LETTER|LEGAL, margin in inches, fontSize, preset executive|modern|bold|minimal|brand, fontFamily sans|serif|mono, header, footer, includePageNumbers, author, workspace-relative logoPath, and six-digit hex primary/accent/text/muted overrides. Font themes use ReportLab built-ins for reliable Daytona rendering; do not supply arbitrary font file names." }, path: { type: "string", description: "Optional workspace-relative .pdf output path" }, name: { type: "string", description: "Optional delivered filename" } }, required: ["title", "sections"] } } },
  { type: "function", function: { name: "CHUCK_START_PHONE_CALL", description: "Start an outbound telephone call through the configured provider (Twilio or Bland). Use only after the user gives an E.164 destination and a clear purpose. This places a real call immediately after normal validation. callProfile selects a personal or business call brief; outbound calls receive only that bounded brief, never the full memory. Optionally attach a voice profile with approved representation details and read-only lookup capabilities; never put passwords, private keys, or payment details in it.", parameters: { type: "object", properties: { phoneNumber: { type: "string", description: "Destination in E.164 format, for example +14155550123" }, purpose: { type: "string", description: "Why Chusky is placing this call" }, callProfile: { type: "string", enum: ["personal", "business"], description: "Whether this call uses the personal or company/business brief" }, profile: { type: "object", description: "Optional profile: identity, organization, mode general|sales|onboarding|support|scheduling, tone professional|warm|direct|consultative, a natural opening, bounded factual context, communication guardrails, and capabilities selected from memory_lookup, scratchpad_lookup, schedule_lookup, task_lookup, call_history. It only narrows read-only voice tools." } }, required: ["phoneNumber", "purpose", "callProfile"] } } },
  { type: "function", function: { name: "CHUCK_LIST_PHONE_CALLS", description: "List your recent Chusky telephone-call attempts. Credentials and audio are never returned.", parameters: { type: "object", properties: {} } } },
  { type: "function", function: { name: "CHUCK_MEETING_CONTEXT_PREPARE", description: "Prepare a private, reviewable client brief before joining a meeting. Search only the owner's normal-sensitivity saved relationship/business memories for the named client; it never joins a meeting, sends anything, or exposes the brief to attendees. Use this when the owner asks Chusky to represent them to a named client, then join without a separate approval unless genuinely decision-critical context or authority is missing.", parameters: { type: "object", properties: { clientName: { type: "string", maxLength: 120 }, objective: { type: "string", maxLength: 1500 }, clientContext: { type: "string", maxLength: 4000, description: "Optional owner-supplied facts or meeting goal; do not include credentials or unrelated private information." } }, required: ["clientName"] } } },
  { type: "function", function: {
    name: "CHUCK_MEETING_JOIN",
      description: "Join a Zoom, Google Meet, Microsoft Teams, or Webex call as Chusky. Before joining in the enabled company-representative role, if the owner has not made clear who the meeting is with or why Chusky is attending, ask them privately for the client/person and objective; do not enter a client meeting unprepared. For a direct link with a known client, provide clientName, objective, and title when known. For a calendar-prepared join, the verified event title plus the owner's representative objective are used to assemble the same bounded private brief automatically. The brief includes only relevant active, normal-sensitivity owner memories and is never sent to Recall or exposed as meeting metadata. By default, participate conversationally and naturally. Recall recording is disabled. For copilot/representative meetings, finalized speech is processed temporarily to prepare the private outcome and then deleted; it is not searchable or retained after outcome processing. Only when the owner explicitly requests a searchable record, set transcriptRetentionDays to 1, 7, or 30; Chusky then retains it in its owner-scoped encrypted store until that expiry, independently of Recall's recording retention. Owners can search bounded excerpts or delete a retained transcript using the meeting transcript tools. If the owner explicitly asks Chusky to inspect shared slides/screens, set analyzeScreenShare=true; this is opt-in, supported only on Zoom/Google Meet/Teams, and uses short-lived snapshots. Do not enable it just because a screen might be shared. An enabled representative can answer from normal-sensitivity company knowledge, use exact connected-app tools already granted in its profile, and capture a participant's volunteered contact details for an agreed follow-up. Use addressed mode only when the owner explicitly requests wake-word-only behavior. Never claim an action succeeded until the tool confirms it.",
      parameters: { type: "object", properties: { meetingUrl: { type: "string", description: "HTTPS meeting link from Zoom, Google Meet, Microsoft Teams, or Webex" }, title: { type: "string", description: "Private meeting label or agenda; used to prepare the owner-grounded brief when direct joining" }, joinAt: { type: "string", description: "Optional future ISO-8601 time, at least 10 minutes and no more than 30 days ahead" }, interactionMode: { type: "string", enum: ["addressed", "copilot", "representative"], description: "Optional override. Use addressed only if the owner explicitly wants wake-word-only behavior. Omit to automatically select the configured company representative or natural conversational participation." }, languageMode: { type: "string", enum: ["english", "multilingual"], description: "Optional voice recognition mode. Multilingual mode uses Flux auto-detection when languageHints is omitted." }, languageHints: { type: "array", items: { type: "string", maxLength: 40 }, maxItems: 8, description: "Optional language codes or names to bias multilingual recognition; omit to allow automatic detection and dynamic language switching." }, keyterms: { type: "array", items: { type: "string", maxLength: 80 }, maxItems: 50, description: "Optional company, product, customer, or acronym vocabulary for recognition." }, liveCaptions: { type: "boolean", description: "Optional explicit opt-in to show transient speaker-labelled captions in the meeting participant surface. Captions are not stored or searchable; use transcriptRetentionDays separately for retained transcripts." }, transcriptRetentionDays: { type: "number", enum: [1, 7, 30], description: "Optional explicit owner opt-in to keep a searchable transcript in Chusky for exactly 1, 7, or 30 days after the meeting. Omit for deletion after outcome processing; never changes Recall recording retention." }, analyzeScreenShare: { type: "boolean", description: "Explicit opt-in to receive and analyze shared-screen PNG frames for this meeting. Use only when the owner asks Chusky to inspect/discuss the screen, slides, or visual demo. Only Zoom, Google Meet, and Microsoft Teams are supported; omit or set false otherwise." }, clientName: { type: "string", maxLength: 120, description: "Name of the client/person for a direct representative join; provide when known so only their owner-approved facts enter the private brief." }, objective: { type: "string", maxLength: 1500, description: "Meeting-specific outcome. Ask the owner privately before joining as their representative if neither the meeting title nor their request makes the purpose clear." }, clientContext: { type: "string", maxLength: 4000, description: "Optional owner-provided context for this specific meeting; never include credentials or unrelated personal information." } }, required: ["meetingUrl"] },
  } },
  { type: "function", function: { name: "CHUCK_MEETING_CONTEXT_LOOKUP", description: "Look up relevant normal-sensitivity company business knowledge during any active enabled representative meeting, plus a specific relationship fact from the named client's owner-requested frozen mission when one exists. Personal/sensitive memories, general inbox/files/tasks, and unrelated clients are excluded.", parameters: { type: "object", properties: { query: { type: "string", maxLength: 500 } }, required: ["query"] } } },
  { type: "function", function: { name: "CHUCK_MEETING_CONTACT_CAPTURE", description: "During an active enabled representative meeting, save a compact private follow-up contact card from details the participant shared for an agreed next step. Capture only the participant's name, email and/or phone, contact preference, stated interest, discussed next step, and an explicitly agreed future follow-up time if there is one. The meeting roster and ambient transcript are not contact lists. An email or phone number is required; do not invent one.", parameters: { type: "object", properties: { participantName: { type: "string", maxLength: 160 }, email: { type: "string", maxLength: 254 }, phone: { type: "string", maxLength: 40 }, contactPreference: { type: "string", enum: ["email", "phone", "unspecified"] }, interest: { type: "string", maxLength: 1000 }, nextStep: { type: "string", maxLength: 500 }, followUpAt: { type: "string", description: "Optional future ISO-8601 date/time the participant agreed to receive a follow-up" } }, required: ["participantName", "interest"] } } },
  { type: "function", function: { name: "CHUCK_MEETING_CONTACTS_LIST", description: "Privately review recent follow-up contacts captured in your meetings. Available only in a private owner conversation; never use this in a group or meeting. Returns only the captured contact card, not transcripts or attendee rosters.", parameters: { type: "object", properties: { limit: { type: "number", minimum: 1, maximum: 50 } } } } },
  { type: "function", function: { name: "CHUCK_MEETING_CONTACT_DELETE", description: "Delete one of your privately stored meeting follow-up contact cards by ID. Available only in a private owner conversation.", parameters: { type: "object", properties: { id: { type: "string" } }, required: ["id"] } } },
  { type: "function", function: { name: "CHUCK_MEETING_FOLLOWUP_SCHEDULE", description: "Schedule one delayed, personalized email for a contact captured in this owned representative meeting. Use only when that participant asked for or agreed to a later follow-up and selected an email address/contact preference. The task is bound to this meeting, that contact, and one exact owner-granted email action; it has no access to general chat history or other tools. Provide the contact ID returned by CHUCK_MEETING_CONTACT_CAPTURE and a future ISO-8601 runAt.", parameters: { type: "object", properties: { contactId: { type: "string" }, runAt: { type: "string", description: "Agreed future ISO-8601 time for the follow-up email" } }, required: ["contactId", "runAt"] } } },
  { type: "function", function: { name: "CHUCK_MEETING_PREPARATION_LIST", description: "List private calendar meeting preparations created from your verified Google Calendar triggers. It returns safe event metadata, recommendation state, and an ID for a later join; it never exposes event descriptions, email contents, or meeting links.", parameters: { type: "object", properties: { limit: { type: "number", minimum: 1, maximum: 20 } } } } },
  { type: "function", function: { name: "CHUCK_MEETING_PREPARATION_JOIN", description: "Join a calendar meeting that Chusky previously prepared from your verified Google Calendar trigger. Use this when the owner says to join a listed/prepared meeting. The private meeting link remains sealed and is never returned. If the event starts at least ten minutes in the future, schedule Chusky for that start time; otherwise join now. This creates a real meeting participant and requires owner approval.", parameters: { type: "object", properties: { id: { type: "string", description: "Calendar preparation ID beginning with cmp_" } }, required: ["id"] } } },
  { type: "function", function: { name: "CHUCK_MEETING_LIST", description: "List your recent Zoom, Google Meet, Microsoft Teams, and Webex assistants and their safe status. Meeting links, transcripts, and provider credentials are never returned.", parameters: { type: "object", properties: { limit: { type: "number", minimum: 1, maximum: 20 } } } } },
  { type: "function", function: { name: "CHUCK_MEETING_STATUS", description: "Check the status of one of your meeting assistants. Meeting ownership is enforced and no meeting URL or transcript is returned.", parameters: { type: "object", properties: { id: { type: "string" } }, required: ["id"] } } },
  { type: "function", function: { name: "CHUCK_MEETING_TRANSCRIPT_SEARCH", description: "Search only your own meeting transcripts that you explicitly opted to retain for 1, 7, or 30 days. Available only in a private owner conversation, never in a group or meeting. Returns at most 10 short matching excerpts with unverified speaker display labels and the transcript expiry; default temporary outcome transcripts are not searchable.", parameters: { type: "object", properties: { query: { type: "string", maxLength: 200 }, meetingId: { type: "string", description: "Optional owned meeting ID to restrict the search" }, limit: { type: "number", minimum: 1, maximum: 10 } }, required: ["query"] } } },
  { type: "function", function: { name: "CHUCK_MEETING_TRANSCRIPT_DELETE", description: "Permanently delete your retained transcript for one ended meeting and disable further search access. Available only in a private owner conversation; does not delete the meeting outcome, scratchpad, or Notion page.", parameters: { type: "object", properties: { meetingId: { type: "string" } }, required: ["meetingId"] } } },
  { type: "function", function: { name: "CHUCK_MEETING_LEAVE", description: "Leave or cancel one of your meeting assistants. Scheduled bots are cancelled before dispatch; joining or in-call bots are asked to leave immediately.", parameters: { type: "object", properties: { id: { type: "string" } }, required: ["id"] } } },
  { type: "function", function: { name: "CHUCK_MEETING_PROFILE_GET", description: "Read your private meeting representative setup: role, objective, approved company facts, authority boundaries, and exact tools Chusky may use in meetings. This does not return credentials or personal memory.", parameters: { type: "object", properties: {} } } },
  { type: "function", function: {
    name: "CHUCK_MEETING_PROFILE_UPDATE",
    description: "Configure Chusky's meeting-representative role, objective, style, approved company knowledge, authority guidance, and exact connected-app/native actions. Composio grants must be exact direct tool slugs; arbitrary discovery/execution and high-impact actions are rejected. This setting is private-owner-only. Set autoJoinCalendar=true only when the owner explicitly asks Chusky to automatically join supported future meetings from their verified Google Calendar; it schedules a real bot without per-event approval and can be disabled to cancel pending automatic joins. This requires enabled=true and an objective.",
    parameters: { type: "object", properties: {
      enabled: { type: "boolean" }, representativeName: { type: "string", maxLength: 80 }, organizationName: { type: "string", maxLength: 120 },
      role: { type: "string", enum: ["sales", "client_onboarding", "employee_onboarding", "customer_success", "custom"] },
      objective: { type: "string", maxLength: 1500 }, communicationStyle: { type: "string", maxLength: 1500 },
      approvedKnowledge: { type: "string", maxLength: 8000, description: "Owner-approved company facts, products, pricing, onboarding steps and FAQs; do not include secrets or unrelated personal data." },
      authorityBoundaries: { type: "string", maxLength: 2000 },
      autoJoinCalendar: { type: "boolean", description: "Explicit owner opt-in. Automatically schedule Chusky for supported Google Calendar meeting events and cancel those future bot joins when disabled." },
      allowedComposioTools: { type: "array", maxItems: 40, items: { type: "string" }, description: "Exact verified direct app action slugs, such as CRM updates, calendar booking, and follow-up email. No Composio meta-tools." },
      composioAccountAliases: { type: "object", maxProperties: 40, additionalProperties: { type: "string", maxLength: 160 }, description: "Owner-selected Composio account alias by action prefix; participants cannot override it." },
      allowedNativeTools: { type: "array", maxItems: 4, items: { type: "string", enum: ["CHUCK_SET_REMINDER", "CHUCK_LIST_REMINDERS", "CHUCK_TASK_CREATE", "CHUCK_TASK_SCHEDULE"] } },
    }, required: [] },
  } },
  { type: "function", function: { name: "CHUCK_GENERATE_IMAGE", description: "Generate or edit one or more images with Chusky's native image studio. Use mode=edit with current or saved reference images to modify an image; use mode=generate for new images. References may be current:0, generated:0, or a saved image asset ID/name. Every generated image is automatically persisted as a durable private R2 image asset and the tool result includes its asset ID and fresh download URL, so later actions such as Instagram uploads can reuse the exact image. Supports count 1-10 and provider-specific studio controls. The default x-ai/grok-imagine-image-2.0 supports 1K/2K resolution, the documented aspect ratios, low/medium quality, one output per request, and up to three input references; unsupported generic controls are filtered automatically. For a standalone image request, leave destination unset (or use telegram) so the result is delivered to the active channel. Use destination=daytona or both only when the image is needed as a workspace/artifact input or the user explicitly asks to save it there; workspacePath must be workspace-relative.", parameters: { type: "object", properties: { prompt: { type: "string", description: "Complete visual brief or edit instruction" }, mode: { type: "string", enum: ["generate", "edit", "reference_variations"], description: "Generation mode; edit requires at least one reference" }, references: { type: "array", items: { type: "string" }, description: "Reference selectors: current:0, generated:0, or a saved image asset ID/name" }, count: { type: "integer", minimum: 1, maximum: 10, description: "Number of images to generate; defaults to 1" }, aspectRatio: { type: "string", description: "Provider-specific aspect ratio; Grok supports 1:1, 3:4, 4:3, 9:16, 16:9, 2:3, 3:2, 9:19.5, 19.5:9, 9:20, 20:9, 1:2, 2:1, or auto" }, resolution: { type: "string", enum: ["512", "1K", "2K", "4K"] }, size: { type: "string", description: "Provider-specific pixel size; unsupported values are omitted by the active model" }, quality: { type: "string", enum: ["auto", "low", "medium", "high"], description: "Grok supports low or medium; high is safely mapped to medium" }, outputFormat: { type: "string", enum: ["png", "jpeg", "webp"] }, background: { type: "string", enum: ["auto", "transparent", "opaque"] }, seed: { type: "integer" }, destination: { type: "string", enum: ["telegram", "daytona", "both"], default: "telegram", description: "Standalone images default to the active channel/Telegram. Use daytona or both only for workspace/artifact work or an explicit save request." }, workspacePath: { type: "string", description: "Workspace-relative output path when destination includes daytona; multiple images receive -1, -2 suffixes" } }, required: ["prompt"] } } },
  { type: "function", function: { name: "CHUCK_CREATE_TRIGGER", description: "Create a Composio trigger for the user.", parameters: { type: "object", properties: { slug: { type: "string" }, triggerConfig: { type: "object", additionalProperties: true } }, required: ["slug"] } } },
  { type: "function", function: { name: "CHUCK_GENERATE_VIDEO", description: "Start native asynchronous video generation. Use references for saved/current/generated images; use frameMode=first_frame or last_frame when the reference must be an exact frame, otherwise use reference guidance. Supports duration, aspect ratio, resolution, exact size, generated audio, and reference images when the selected video model supports them. By default send the completed video to Telegram. Daytona paths must be workspace-relative.", parameters: { type: "object", properties: { prompt: { type: "string", description: "Complete video brief" }, references: { type: "array", items: { type: "string" }, description: "Reference selectors: current:0, generated:0, or saved image asset ID/name" }, frameMode: { type: "string", enum: ["reference", "first_frame", "last_frame"] }, duration: { type: "integer", minimum: 1, maximum: 30 }, aspectRatio: { type: "string" }, resolution: { type: "string", enum: ["480p", "720p", "1080p", "2K"] }, size: { type: "string", description: "Exact pixel size such as 1280x720" }, generateAudio: { type: "boolean" }, destination: { type: "string", enum: ["telegram", "daytona", "both"], description: "Where the completed result goes; defaults to telegram" }, workspacePath: { type: "string", description: "Workspace-relative output path used only when destination is daytona or both" } }, required: ["prompt"] } } },
  { type: "function", function: { name: "CHUCK_CREATE_PRESENTATION", description: "Create and deliver a polished, valid PowerPoint through Chusky's built-in OOXML generator. It does not download Python packages inside Daytona. Use this for new PPTX decks instead of hand-writing XML. It adds a designed cover, reusable masters, consistent typography, accessible image alt text, slide numbers, styled tables, readable charts, layout-aware spacing, and a supplied brand logo on every slide. Supports a cover plus 1-30 structured slides. Each slide requires title and can include body, bullets, imagePaths, imageAltTexts, imageFit (contain or cover), backgroundImagePath, backgroundImageAltText, overlayColor, overlayOpacity (0-100), textColor, table, chart, metrics, quote, eyebrow, notes, and layout. Layouts: auto, title, section, two_column, hero, metrics, comparison, timeline, image_focus, background, chart, table, quote, closing. Use backgroundImagePath for a full-bleed image; the generator preserves its aspect ratio with cover fitting, adds a configurable readability scrim and left text safe zone, and chooses a high-contrast foreground color unless textColor is supplied. Do not provide backgroundImagePath and imagePaths on the same slide. A table can be a cell matrix or { headers: string[], rows: string[][] }; empty optional tables are ignored. Each slide supports either a table or a chart, not both. Use the shared brand object for companyName, tagline, logoPath, style preset, and palette. Explicit style values override brand values. The deck is structurally validated, render-checked, then delivered as an artifact.", parameters: { type: "object", properties: { title: { type: "string" }, slides: { type: "array", items: { type: "object" }, description: "Each slide requires title. Optional fields: body, bullets, imagePaths, imageAltTexts, imageFit, backgroundImagePath, backgroundImageAltText, overlayColor, overlayOpacity, textColor, table, chart, metrics [{label,value,detail}], quote, eyebrow, notes, accent, and layout." }, brand: { type: "object", description: "Reusable company identity: companyName, tagline, workspace-relative logoPath, header/footer, preset executive|modern|bold|minimal|brand, fontFamily sans|serif|mono, and six-digit hex colors." }, style: { type: "object", description: "Optional design system: preset executive|modern|bold|minimal|brand; six-digit hex color overrides; fontFace; headingFontFace; footer; includeSlideNumbers; workspace-relative logoPath." }, path: { type: "string", description: "Optional workspace-relative .pptx output path" }, name: { type: "string", description: "Optional delivered filename" } }, required: ["title", "slides"] } } },
  { type: "function", function: { name: "CHUCK_CREATE_DOCUMENT", description: "Create and deliver a genuine, styled DOCX in Daytona. Use instead of a hand-written DOCX script when the user needs a professional Word document. It supports headings, rich body text, bullets, banded tables, images, captions, page breaks, a letterhead header, top-right company logo, footer, and shared brand palettes. The DOCX is structurally validated and render-checked before delivery.", parameters: { type: "object", properties: { title: { type: "string" }, sections: { type: "array", items: { type: "object" }, description: "Same structured sections as CHUCK_CREATE_PDF: heading, body, bullets, table or {headers,rows}, imagePath, imageAltText, imageWidth, and pageBreakBefore." }, brand: { type: "object", description: "Reusable company identity: companyName, tagline, workspace-relative logoPath, header/footer, preset executive|modern|bold|minimal|brand, fontFamily sans|serif|mono, and six-digit hex colors." }, style: { type: "object", description: "Optional explicit style overrides. Use workspace-relative logoPath. Explicit style values override brand values." }, path: { type: "string", description: "Optional workspace-relative .docx output path" }, name: { type: "string" } }, required: ["title", "sections"] } } },
  { type: "function", function: { name: "CHUCK_CREATE_SPREADSHEET", description: "Create and deliver a genuine branded XLSX workbook in Daytona. Use this for polished data workbooks rather than registering an unverified script output. Each sheet receives a company header, optional top-right PNG/JPEG logo, tab color, frozen headers, fitted columns, styled banded tables, and the shared brand palette. For an advanced native Excel chart not expressible here, create the verified data workbook first and then use Daytona to add it; never claim a chart exists unless it is in the returned workbook.", parameters: { type: "object", properties: { title: { type: "string" }, sheets: { type: "array", items: { type: "object" }, description: "1-12 sheets. Each needs name and rows as a cell matrix or {headers,rows}; optional tabColor is a six-digit hex color." }, brand: { type: "object", description: "Reusable company identity: companyName, tagline, workspace-relative PNG/JPEG logoPath, preset executive|modern|bold|minimal|brand, fontFamily, and six-digit hex colors." }, style: { type: "object", description: "Optional explicit palette/style overrides. Explicit style values override brand values." }, path: { type: "string", description: "Optional workspace-relative .xlsx output path" }, name: { type: "string" } }, required: ["title", "sheets"] } } },
  { type: "function", function: { name: "CHUCK_VIDEO_STATUS", description: "Check the status of your recent asynchronous video-generation jobs. Use an optional job ID for one exact job; this is read-only and never starts or cancels generation.", parameters: { type: "object", properties: { id: { type: "string", description: "Optional video job ID" }, limit: { type: "number", minimum: 1, maximum: 10, description: "Maximum recent jobs to return; defaults to 5" } } } } },
  { type: "function", function: { name: "CHUCK_LIST_CONNECTED_ACCOUNTS", description: "List your owner-scoped Composio OAuth accounts using safe metadata only. It returns toolkit, alias, status, and account ID; it never returns tokens, credentials, provider payloads, or raw connection data. Use this before choosing an account for connected-app work.", parameters: { type: "object", properties: { toolkit: { type: "string", description: "Optional Composio toolkit slug such as gmail, slack, or hubspot" }, limit: { type: "number", minimum: 1, maximum: 50, description: "Maximum accounts to return; defaults to 20" } } } } },
  { type: "function", function: { name: "CHUCK_SET_REMINDER", description: "Set a durable one-time reminder. notify delivers the text; check_in asks the agent to review the linked context; act lets it perform a bounded autonomous slice; wait_until parks it until the linked condition is ready. Provide delaySeconds or a future ISO runAt.", parameters: { type: "object", properties: { text: { type: "string" }, delaySeconds: { type: "number" }, runAt: { type: "string", description: "Future ISO-8601 timestamp" }, mode: { type: "string", enum: ["notify", "check_in", "act", "wait_until"] }, links: { type: "object", additionalProperties: { type: "string" }, description: "Optional owner-scoped task, mission, open-loop, project, meeting, or conversation references" }, nextAction: { type: "string", maxLength: 1000 }, pollEverySeconds: { type: "number", minimum: 60, maximum: 604800, description: "For wait_until only: re-check interval" }, preconditions: { type: "array", maxItems: 8, items: { type: "string", maxLength: 500 } }, postconditions: { type: "array", maxItems: 8, items: { type: "string", maxLength: 500 } } }, required: ["text"] } } },
  { type: "function", function: { name: "CHUCK_LIST_REMINDERS", description: "List the user's active reminders.", parameters: { type: "object", properties: {} } } },
  { type: "function", function: { name: "CHUCK_CANCEL_REMINDER", description: "Cancel one of the user's reminders by ID.", parameters: { type: "object", properties: { id: { type: "string" } }, required: ["id"] } } },
  { type: "function", function: { name: "CHUCK_PAUSE_REMINDER", description: "Pause one of the user's reminders without losing its scheduled time or context.", parameters: { type: "object", properties: { id: { type: "string" } }, required: ["id"] } } },
  { type: "function", function: { name: "CHUCK_RESUME_REMINDER", description: "Resume one of the user's paused reminders from its saved schedule.", parameters: { type: "object", properties: { id: { type: "string" } }, required: ["id"] } } },
  { type: "function", function: { name: "CHUCK_RUN_REMINDER_NOW", description: "Run one of the user's active or paused reminders immediately as a durable delivery.", parameters: { type: "object", properties: { id: { type: "string" } }, required: ["id"] } } },
  { type: "function", function: { name: "CHUCK_SCHEDULE_JOB", description: "Schedule a recurring job using a 5-field CRON expression. notify sends the configured text; check_in or act executes a bounded agent slice on each occurrence. When a specialist calls this, the schedule is permanently bound to that specialist's typed contract. Use text as the concrete recurring objective.", parameters: { type: "object", properties: { text: { type: "string", description: "Concrete objective to execute on every occurrence" }, cron: { type: "string", description: "5-field CRON expression; include CRON_TZ=<IANA timezone> when needed" }, mode: { type: "string", enum: ["notify", "check_in", "act"] }, links: { type: "object", additionalProperties: { type: "string" }, description: "Optional owner-scoped task, mission, open-loop, project, meeting, or conversation references" }, nextAction: { type: "string", maxLength: 1000 }, preconditions: { type: "array", maxItems: 8, items: { type: "string", maxLength: 500 } }, postconditions: { type: "array", maxItems: 8, items: { type: "string", maxLength: 500 } } }, required: ["text", "cron"] } } },
  { type: "function", function: { name: "CHUCK_LIST_JOBS", description: "List the user's active and paused recurring jobs.", parameters: { type: "object", properties: {} } } },
  { type: "function", function: { name: "CHUCK_PAUSE_JOB", description: "Pause one of the user's recurring jobs. Existing work is preserved and future occurrences stop until resumed.", parameters: { type: "object", properties: { id: { type: "string" } }, required: ["id"] } } },
  { type: "function", function: { name: "CHUCK_RESUME_JOB", description: "Resume one of the user's paused recurring jobs without creating a duplicate schedule.", parameters: { type: "object", properties: { id: { type: "string" } }, required: ["id"] } } },
  { type: "function", function: { name: "CHUCK_RUN_JOB_NOW", description: "Run one active recurring job immediately as a separate durable occurrence.", parameters: { type: "object", properties: { id: { type: "string" } }, required: ["id"] } } },
  { type: "function", function: { name: "CHUCK_CANCEL_JOB", description: "Cancel one of the user's recurring jobs by ID.", parameters: { type: "object", properties: { id: { type: "string" } }, required: ["id"] } } },
  { type: "function", function: { name: "CHUCK_SCRATCHPAD_WRITE", description: "Save a private per-user working note for later turns.", parameters: { type: "object", properties: { key: { type: "string" }, content: { type: "string" } }, required: ["key", "content"] } } },
  { type: "function", function: { name: "CHUCK_SCRATCHPAD_READ", description: "Read private scratchpad notes, optionally filtered by a search query. Meeting outcomes are saved under keys like meeting:<meeting-id>; read those notes when the user asks about a past meeting, its decisions, or follow-up actions.", parameters: { type: "object", properties: { query: { type: "string" } } } } },
  { type: "function", function: { name: "CHUCK_SCRATCHPAD_CLEAR", description: "Clear one private scratchpad note or the entire scratchpad.", parameters: { type: "object", properties: { key: { type: "string" } } } } },
  { type: "function", function: { name: "CHUCK_SAVE_MEMORY", description: "Save an explicit durable memory in the correct category. Use profile/personal/preferences for the user, business for work context, relationship for a person, project for a mission, procedural for an approved playbook, episodic for a meaningful event, document for sourced file knowledge, and negative for a do-not rule. Never save casual conversation or secrets unless explicitly requested. You must classify sensitivity as normal or sensitive on every save; use reviewAt or expiresAt when the fact may go stale.", parameters: { type: "object", properties: { category: { type: "string", enum: ["profile", "personal", "preference", "business", "relationship", "project", "procedural", "episodic", "document", "negative", "fact", "instruction"] }, key: { type: "string" }, value: { type: "string" }, source: { type: "string" }, confidence: { type: "number", minimum: 0, maximum: 1 }, sensitivity: { type: "string", enum: ["normal", "sensitive"] }, projectId: { type: "string" }, personKey: { type: "string" }, reviewAt: { type: "number" }, expiresAt: { type: "number" } }, required: ["category", "key", "value", "sensitivity"] } } },
  { type: "function", function: { name: "CHUCK_UPDATE_MEMORY", description: "Update an existing durable memory when the user clearly corrects, changes, or evolves it. Identify the record by id or key; use category to disambiguate a key. Preserve the original memory identity and update only the changed fields. Never update based on an uncertain guess.", parameters: { type: "object", properties: { id: { type: "string" }, key: { type: "string" }, category: { type: "string", enum: ["profile", "personal", "preference", "business", "relationship", "project", "procedural", "episodic", "document", "negative", "fact", "instruction"] }, newCategory: { type: "string", enum: ["profile", "personal", "preference", "business", "relationship", "project", "procedural", "episodic", "document", "negative", "fact", "instruction"] }, newKey: { type: "string" }, value: { type: "string" }, source: { type: "string" }, confidence: { type: "number", minimum: 0, maximum: 1 }, sensitivity: { type: "string", enum: ["normal", "sensitive"] }, projectId: { type: "string" }, personKey: { type: "string" }, reviewAt: { type: "number" }, expiresAt: { type: "number" } }, required: ["value"] } } },
  { type: "function", function: { name: "CHUCK_SAVE_IMAGE_ASSET", description: "Save a current user-provided image or an image generated earlier in this run as a durable private image asset in R2. Use when the user asks to remember, save, or designate an image such as a logo, banner, product reference, or brand asset. Include a stable name, purpose, and factual description.", parameters: { type: "object", properties: { name: { type: "string" }, purpose: { type: "string" }, description: { type: "string" }, tags: { type: "array", items: { type: "string" } }, source: { type: "string", enum: ["current", "generated"], description: "Whether to save the current user image or an image generated earlier in this run; defaults to current" }, sourceIndex: { type: "number", minimum: 0, description: "Zero-based image index; defaults to 0" } }, required: ["name", "purpose"] } } },
  { type: "function", function: { name: "CHUCK_SEARCH_IMAGE_ASSETS", description: "Search the user's saved private image assets by name, purpose, tags, or visual description. Results return metadata and asset IDs, not raw image bytes.", parameters: { type: "object", properties: { query: { type: "string" }, limit: { type: "number", minimum: 1, maximum: 10 } } } } },
  { type: "function", function: { name: "CHUCK_GET_IMAGE_ASSET", description: "Retrieve one saved private image asset by ID or exact name. The returned image is supplied back to the vision-capable model for inspection; do not expose its private URL to the user unless explicitly needed.", parameters: { type: "object", properties: { id: { type: "string" } }, required: ["id"] } } },
  { type: "function", function: { name: "CHUCK_FORGET_IMAGE_ASSET", description: "Permanently remove a saved image asset from private R2 storage and its Vector index when the user explicitly asks.", parameters: { type: "object", properties: { id: { type: "string" } }, required: ["id"] } } },
  { type: "function", function: { name: "CHUCK_SEARCH_MEMORY", description: "Search only the user's relevant saved memories. A query is preferred; never request or return the entire memory store. Filter by category, projectId, or personKey when the context identifies one. Results are bounded and exclude expired records.", parameters: { type: "object", properties: { query: { type: "string" }, category: { type: "string", enum: ["profile", "personal", "preference", "business", "relationship", "project", "procedural", "episodic", "document", "negative", "fact", "instruction"] }, projectId: { type: "string" }, personKey: { type: "string" }, limit: { type: "number", minimum: 1, maximum: 20 } } } } },
  { type: "function", function: { name: "CHUCK_FORGET_MEMORY", description: "Forget a saved memory by key or ID when explicitly requested.", parameters: { type: "object", properties: { key: { type: "string" } }, required: ["key"] } } },
  { type: "function", function: { name: "CHUCK_ATTENTION_STATE", description: "Read or explicitly update Chusky's durable attention state: observations, open loops, attention candidates, standing orders, delivery preferences, relationships, project states, personal/business autonomy watches, and autonomy profiles. New autonomy watches default to personal mode; set mode=business for a company watch so the matching profile and policy are applied. Do not invent monitoring or permissions; only persist a watch or profile when the owner explicitly asks for it.", parameters: { type: "object", properties: {
    action: { type: "string", enum: ["create", "list", "update"] }, kind: { type: "string", enum: ["observation", "open_loop", "attention_candidate", "standing_order", "delivery_preference", "relationship", "project_state", "autonomy_watch", "autonomy_profile"] }, id: { type: "string" }, query: { type: "string" }, status: { type: "string" }, limit: { type: "number" },
    source: { type: "string" }, eventType: { type: "string" }, summary: { type: "string" }, title: { type: "string" }, objective: { type: "string" }, dueAt: { type: "number" }, priority: { type: "number" }, confidence: { type: "number" }, importance: { type: "number" }, novelty: { type: "number" }, reason: { type: "string" }, proposedAction: { type: "string" }, candidateType: { type: "string", enum: ["nudge", "digest", "prepare", "ask", "act"] },
    name: { type: "string" }, instruction: { type: "string" }, authority: { type: "string", enum: ["observe", "prepare", "execute_reversible"] }, scope: { type: "array", items: { type: "string" } }, provider: { type: "string" }, conversationId: { type: "string" }, enabled: { type: "boolean" }, mode: { type: "string", enum: ["immediate", "digest", "silent", "personal", "business"] },
    personKey: { type: "string" }, projectKey: { type: "string" }, role: { type: "string" }, notes: { type: "string" }, currentPhase: { type: "string" }, nextAction: { type: "string" }, blockers: { type: "array", items: { type: "string" } }, lastInteractionAt: { type: "number" }, lastActivityAt: { type: "number" },
    domain: { type: "string" }, toolkit: { type: "string" }, connectedAccountId: { type: "string", maxLength: 200 }, accountAlias: { type: "string", maxLength: 120 }, queryText: { type: "string" }, toolSlugs: { type: "array", items: { type: "string", pattern: "^[A-Z][A-Z0-9]{1,31}_[A-Z0-9_]+$" }, maxItems: 20 }, cursor: { type: "string" }, cadenceSeconds: { type: "number", minimum: 300, maximum: 2592000 }, maxItems: { type: "number", minimum: 1, maximum: 100 }, maxChecksPerDay: { type: "number", minimum: 0, maximum: 1000 }, maxAutonomousActionsPerDay: { type: "number", minimum: 0, maximum: 1000 }, notifyOn: { type: "string", enum: ["important", "changes", "all", "silent"] }, allowedDomains: { type: "array", items: { type: "string" } }, deniedDomains: { type: "array", items: { type: "string" } }, quietHoursUtc: { type: "object" },
  }, required: ["action", "kind"] } } },
  { type: "function", function: { name: "CHUCK_AUTONOMY_STATUS", description: "Read the owner-scoped autonomy control plane: personal profile, active monitoring watches, unfinished commitments, durable tasks/missions, reminders/jobs, meeting preparations, blockers, and the next checks. This is read-only and never executes external actions.", parameters: { type: "object", properties: {} } } },
  { type: "function", function: { name: "CHUCK_AUTONOMY_RECONCILE", description: "Run due owner-configured autonomy watches against exact read-only connected-app tools, update checkpoints, and create deduplicated business-gap candidates. It never sends messages, spends money, edits records, deletes data, or changes permissions. Use only in a private owner context or from the attention pulse.", parameters: { type: "object", properties: { mode: { type: "string", enum: ["personal", "business"] }, maxWatches: { type: "number", minimum: 1, maximum: 20 } } } } },
  { type: "function", function: { name: "CHUCK_AUTONOMY_PLAYBOOK", description: "Plan or start the reusable, evidence-backed playbook for a verified business gap. Starting creates an idempotent durable mission; all consequential external actions remain behind their existing approval boundaries.", parameters: { type: "object", properties: { action: { type: "string", enum: ["plan", "start"] }, gap: { type: "object", properties: { key: { type: "string" }, type: { type: "string", enum: ["overdue_invoice", "unreplied_message", "stale_lead", "staffing_coverage", "stalled_onboarding", "missed_follow_up"] }, severity: { type: "string", enum: ["low", "medium", "high", "critical"] }, title: { type: "string" }, reason: { type: "string" }, recommendedNextAction: { type: "string" }, requiresApproval: { type: "boolean" }, evidence: { type: "array" }, detectedAt: { type: "number" } }, required: ["key", "type", "title", "reason", "recommendedNextAction"] }, input: { type: "object" } }, required: ["action", "gap"] } } },
  { type: "function", function: { name: "CHUCK_ATTENTION_PULSE", description: "Enable, disable, or inspect the durable proactive attention pulse. The pulse reviews the user's bounded open loops, attention candidates, and active standing orders on a QStash schedule, then lets Elena prepare or perform authorized routine work and deliver an owner-scoped digest. Enabling it requires the user to explicitly request proactive monitoring; it never bypasses normal approvals.", parameters: { type: "object", properties: {
    action: { type: "string", enum: ["enable", "disable", "status"] },
    cron: { type: "string", description: "Optional 5-field CRON expression; defaults to hourly at minute 0. Include CRON_TZ=<IANA timezone> when needed." },
  }, required: ["action"] } } },
  { type: "function", function: { name: "CHUCK_TASK_CREATE", description: "Create a durable, resumable task for the user. Use before beginning work that may span multiple turns or require a computer workspace.", parameters: { type: "object", properties: { title: { type: "string" }, objective: { type: "string" }, workspaceId: { type: "string" } }, required: ["title", "objective"] } } },
  { type: "function", function: { name: "CHUCK_TASK_LIST", description: "List the user's durable tasks. Optionally filter by one or more lifecycle statuses.", parameters: { type: "object", properties: { statuses: { type: "array", items: { type: "string", enum: ["queued", "running", "blocked", "completed", "failed", "cancelled"] } } } } } },
  { type: "function", function: { name: "CHUCK_TASK_GET", description: "Read a durable task, including its latest checkpoint and next action.", parameters: { type: "object", properties: { id: { type: "string" } }, required: ["id"] } } },
  { type: "function", function: { name: "CHUCK_TASK_CHECKPOINT", description: "Persist concise progress and the exact next action for a running task. Use after meaningful work and before ending a turn.", parameters: { type: "object", properties: { id: { type: "string" }, checkpoint: { type: "string" }, nextAction: { type: "string" } }, required: ["id", "checkpoint"] } } },
  { type: "function", function: { name: "CHUCK_TASK_BLOCK", description: "Record why an unfinished durable task cannot proceed and what is needed next. Use when a dependency, permission, or decision blocks progress.", parameters: { type: "object", properties: { id: { type: "string" }, reason: { type: "string" }, nextAction: { type: "string" } }, required: ["id", "reason"] } } },
  { type: "function", function: { name: "CHUCK_TASK_COMPLETE", description: "Mark a durable task complete only after its objective is achieved, with a concise result.", parameters: { type: "object", properties: { id: { type: "string" }, result: { type: "string" } }, required: ["id", "result"] } } },
  { type: "function", function: { name: "CHUCK_TASK_CANCEL", description: "Cancel one of the user's unfinished durable tasks.", parameters: { type: "object", properties: { id: { type: "string" } }, required: ["id"] } } },
  { type: "function", function: { name: "CHUCK_TASK_RETRY", description: "Requeue a failed, blocked, or cancelled durable task while preserving its checkpoint.", parameters: { type: "object", properties: { id: { type: "string" } }, required: ["id"] } } },
  { type: "function", function: { name: "CHUCK_TASK_SCHEDULE", description: "Schedule an existing durable task for a future time. Use only when the user explicitly asks Chusky to continue it later.", parameters: { type: "object", properties: { id: { type: "string" }, delaySeconds: { type: "number" }, runAt: { type: "string", description: "Future ISO-8601 timestamp" } }, required: ["id"] } } },
  { type: "function", function: { name: "CHUCK_TASK_WAIT", description: "Internal durable continuation only. Pause the active task until a future time, persist the checkpoint and exact next action, then wake the same task workflow. This does not notify the user, create a reminder, or start a recurring job. Use it when an external service is still processing and the task should check again later. It is terminal for the current task turn; do not call more tools after it.", parameters: { type: "object", properties: { delaySeconds: { type: "number", minimum: 60, maximum: 604800, description: "Wait at least 60 seconds and no more than 7 days." }, runAt: { type: "string", description: "Future ISO-8601 timestamp, at least 60 seconds from now and no more than 7 days ahead." }, checkpoint: { type: "string", maxLength: 4000, description: "What has been verified so far." }, nextAction: { type: "string", maxLength: 1000, description: "The exact action to perform after wake-up." }, reason: { type: "string", maxLength: 500, description: "Why the task is waiting." } }, required: ["checkpoint", "nextAction"] } } },
  { type: "function", function: { name: "CHUCK_MISSION_START", description: "Start a durable autonomous mission. Use for multi-step work that should continue across bounded execution slices, survive restarts, and resume from checkpoints. State a concrete definition of done and bounded budgets. Optional steps form a dependency-aware mission plan; independent steps may run in parallel.", parameters: { type: "object", properties: { title: { type: "string", maxLength: 240 }, objective: { type: "string", maxLength: 8000 }, definitionOfDone: { type: "string", maxLength: 4000 }, requiredEvidence: { type: "array", items: { type: "string", maxLength: 500 }, maxItems: 50 }, verificationMode: { type: "string", enum: ["legacy", "strict"] }, steps: { type: "array", maxItems: 100, items: { type: "object", properties: { id: { type: "string", maxLength: 160 }, title: { type: "string", maxLength: 240 }, objective: { type: "string", maxLength: 4000 }, dependsOn: { type: "array", items: { type: "string", maxLength: 160 }, maxItems: 20 }, retryLimit: { type: "number", minimum: 0, maximum: 20 }, input: { type: "object" }, outputSchema: { type: "object" }, evidenceRequired: { type: "array", items: { type: "string", maxLength: 500 }, maxItems: 20 }, compensationObjective: { type: "string", maxLength: 4000 }, retryBackoffSeconds: { type: "number", minimum: 0, maximum: 86400 }, parallelGroup: { type: "string", maxLength: 120 } }, required: ["title", "objective"] } }, maxDurationSeconds: { type: "number", minimum: 60, maximum: 2592000 }, maxSteps: { type: "number", minimum: 1, maximum: 1000 }, maxToolCalls: { type: "number", minimum: 1, maximum: 10000 }, maxCost: { type: "number", minimum: 0, maximum: 10000 }, idempotencyKey: { type: "string", maxLength: 200 } }, required: ["title", "objective", "definitionOfDone"] } } },
  { type: "function", function: { name: "CHUCK_MISSION_LIST", description: "List the user's autonomous missions and their honest lifecycle state, checkpoint, next action, and budget consumption.", parameters: { type: "object", properties: { statuses: { type: "array", items: { type: "string", enum: ["queued", "running", "waiting", "paused", "blocked", "completed", "failed", "cancelled"] } } } } } },
  { type: "function", function: { name: "CHUCK_MISSION_GET", description: "Inspect one owned autonomous mission, including checkpoint, next action, budget, steps, and bounded event history.", parameters: { type: "object", properties: { id: { type: "string" } }, required: ["id"] } } },
  { type: "function", function: { name: "CHUCK_MISSION_PROOF", description: "Show a bounded proof record for an owned mission: definition of done, step results, evidence, verification state, budget, and recent lifecycle events.", parameters: { type: "object", properties: { id: { type: "string" } }, required: ["id"] } } },
  { type: "function", function: { name: "CHUCK_MISSION_CHECKPOINT", description: "Save verified mission progress and the exact next action. Do this before ending a bounded slice; never claim the mission is complete from a checkpoint alone.", parameters: { type: "object", properties: { id: { type: "string" }, checkpoint: { type: "string", maxLength: 8000 }, nextAction: { type: "string", maxLength: 2000 } }, required: ["id", "checkpoint"] } } },
  { type: "function", function: { name: "CHUCK_MISSION_WAIT_EVENT", description: "Park a mission until one exact provider event arrives. Use the provider event's stable id; duplicate deliveries are ignored and do not resume another mission.", parameters: { type: "object", properties: { id: { type: "string" }, provider: { type: "string", maxLength: 120 }, providerEventId: { type: "string", maxLength: 240 }, stepId: { type: "string", maxLength: 160 }, checkpoint: { type: "string", maxLength: 8000 }, nextAction: { type: "string", maxLength: 2000 }, timeoutSeconds: { type: "number", minimum: 60, maximum: 2592000 } }, required: ["id", "provider", "providerEventId"] } } },
  { type: "function", function: { name: "CHUCK_MISSION_STEP_COMPLETE", description: "Mark one verified mission step complete and advance only to steps whose dependencies are complete. Do not use this without evidence for the step result.", parameters: { type: "object", properties: { id: { type: "string" }, stepId: { type: "string", maxLength: 160 }, result: { type: "string", maxLength: 12000 } }, required: ["id", "stepId", "result"] } } },
  { type: "function", function: { name: "CHUCK_MISSION_REPLAN", description: "Replace only the unfinished portion of a mission plan when verified information changes. Completed steps are preserved; dependencies are validated before the next ready step is selected.", parameters: { type: "object", properties: { id: { type: "string" }, reason: { type: "string", maxLength: 2000 }, steps: { type: "array", minItems: 1, maxItems: 100, items: { type: "object", properties: { id: { type: "string", maxLength: 160 }, title: { type: "string", maxLength: 240 }, objective: { type: "string", maxLength: 4000 }, dependsOn: { type: "array", items: { type: "string", maxLength: 160 }, maxItems: 20 }, retryLimit: { type: "number", minimum: 0, maximum: 20 } }, required: ["title", "objective"] } } }, required: ["id", "reason", "steps"] } } },
  { type: "function", function: { name: "CHUCK_MISSION_PAUSE", description: "Pause an owned autonomous mission without losing its checkpoint.", parameters: { type: "object", properties: { id: { type: "string" }, reason: { type: "string", maxLength: 2000 } }, required: ["id"] } } },
  { type: "function", function: { name: "CHUCK_MISSION_RESUME", description: "Resume an owned paused, blocked, or failed autonomous mission from its last verified checkpoint. For a mission waiting on approval, continue only if its exact persisted approval is already approved and unexpired; this resumes the original durable task. Never treat a chat message as approval or bypass a pending gate.", parameters: { type: "object", properties: { id: { type: "string" } }, required: ["id"] } } },
  { type: "function", function: { name: "CHUCK_MISSION_CANCEL", description: "Cancel an owned autonomous mission and stop future continuation slices.", parameters: { type: "object", properties: { id: { type: "string" }, reason: { type: "string", maxLength: 2000 } }, required: ["id"] } } },
  { type: "function", function: { name: "CHUCK_MISSION_BLOCK", description: "Block an autonomous mission when a dependency, permission, or human decision prevents safe progress. Preserve the verified checkpoint and exact next action.", parameters: { type: "object", properties: { id: { type: "string" }, reason: { type: "string", maxLength: 2000 }, nextAction: { type: "string", maxLength: 2000 } }, required: ["id", "reason"] } } },
  { type: "function", function: { name: "CHUCK_MISSION_COMPLETE", description: "Complete an autonomous mission only after its definition of done has been verified and the result is available.", parameters: { type: "object", properties: { id: { type: "string" }, result: { type: "string", maxLength: 12000 } }, required: ["id", "result"] } } },
  { type: "function", function: { name: "CHUCK_MISSION_EVIDENCE", description: "Attach bounded, source-backed evidence to an owned mission or step. Evidence can be a source, tool receipt, artifact, assertion, before/after record, or human confirmation. Server-observed system evidence is recorded automatically from trusted execution receipts.", parameters: { type: "object", properties: { id: { type: "string" }, stepId: { type: "string" }, evidence: { type: "array", maxItems: 20, items: { type: "object", properties: { kind: { type: "string", enum: ["source", "tool_receipt", "artifact", "assertion", "before_after", "human_confirmation"] }, summary: { type: "string", maxLength: 2000 }, source: { type: "string", maxLength: 500 }, ref: { type: "string", maxLength: 500 }, hash: { type: "string", maxLength: 128 }, verified: { type: "boolean" }, verifiedBy: { type: "string", enum: ["agent", "human"] } }, required: ["kind", "summary", "verified"] } } }, required: ["id", "evidence"] } } },
  { type: "function", function: { name: "CHUCK_MISSION_VERIFY", description: "Verify an owned mission's definition of done against completed steps, trusted receipts, and optional provider-backed outcome checks. Provider reads run through currently available read-only tools; the model cannot supply pass results. Never claim completion when a required check is stale, failed, or uncertain.", parameters: { type: "object", properties: { id: { type: "string" }, evidenceIds: { type: "array", items: { type: "string" }, maxItems: 50 }, confidence: { type: "number", minimum: 0, maximum: 1 }, checks: { type: "array", maxItems: 50, items: { type: "object", properties: { id: { type: "string" }, kind: { type: "string", enum: ["provider_read", "receipt", "artifact", "human"] }, description: { type: "string" }, provider: { type: "string" }, toolSlug: { type: "string" }, arguments: { type: "object", description: "Bounded non-secret arguments for this exact provider read action." }, freshnessMs: { type: "number" }, required: { type: "boolean" }, expected: { type: "object" } }, required: ["id", "kind", "description"] } } }, required: ["id"], additionalProperties: false } } },
  { type: "function", function: { name: "CHUCK_MISSION_COMPENSATE", description: "Inspect or approve a durable compensation record created after an ambiguous external action. Approval never claims provider success; the provider-specific supervisor must execute and return a receipt.", parameters: { type: "object", properties: { compensationId: { type: "string" }, action: { type: "string", enum: ["inspect", "approve"] } }, required: ["compensationId", "action"], additionalProperties: false } } },
  { type: "function", function: { name: "CHUCK_MISSION_REPAIR", description: "Put an owned mission into an honest repair state after a failed assertion, provider error, or inconsistent result, preserving completed work and a concrete recovery action.", parameters: { type: "object", properties: { id: { type: "string" }, reason: { type: "string", maxLength: 2000 }, nextAction: { type: "string", maxLength: 2000 } }, required: ["id", "reason"] } } },
  { type: "function", function: { name: "CHUCK_CONTEXT_SEARCH", description: "Select owner-scoped context for a purpose instead of dumping all memory. Supports organization, department, project, mission, meeting, channel, and conversation scopes.", parameters: { type: "object", properties: { query: { type: "string", maxLength: 500 }, scope: { type: "string", enum: ["user", "organization", "department", "project", "mission", "meeting", "conversation", "channel"] }, scopeId: { type: "string", maxLength: 180 }, purpose: { type: "string", enum: ["planning", "execution", "meeting", "support", "sales", "reporting", "handoff"] }, limit: { type: "number", minimum: 1, maximum: 100 } } } } },
  { type: "function", function: { name: "CHUCK_CONTEXT_SAVE", description: "Save a durable, owner-scoped context node such as a decision, objective, preference, open loop, tool receipt, artifact, or department fact. Never save secrets unless explicitly requested.", parameters: { type: "object", properties: { scope: { type: "string", enum: ["user", "organization", "department", "project", "mission", "meeting", "conversation", "channel"] }, scopeId: { type: "string" }, kind: { type: "string", enum: ["memory", "decision", "preference", "objective", "open_loop", "tool_receipt", "artifact", "meeting", "message", "fact", "relationship"] }, key: { type: "string", maxLength: 240 }, value: { type: "string", maxLength: 20000 }, source: { type: "string" }, sourceRef: { type: "string" }, sensitivity: { type: "string", enum: ["normal", "sensitive"] }, confidence: { type: "number", minimum: 0, maximum: 1 }, tags: { type: "array", items: { type: "string" }, maxItems: 20 }, reviewAt: { type: "number" }, expiresAt: { type: "number" } }, required: ["scope", "kind", "key", "value", "sensitivity"] } } },
  { type: "function", function: { name: "CHUCK_DEPARTMENT_HANDOFF", description: "Create a typed, owner-scoped work packet for another department or specialist. Include inputs, constraints, evidence requirements, output schema, deadline, and approval boundary.", parameters: { type: "object", properties: { department: { type: "string" }, objective: { type: "string", maxLength: 4000 }, inputs: { type: "object" }, constraints: { type: "array", items: { type: "string" }, maxItems: 30 }, evidenceRequired: { type: "array", items: { type: "string" }, maxItems: 30 }, outputSchema: { type: "object" }, toAgent: { type: "string" }, deadline: { type: "number" }, approvalBoundary: { type: "string", maxLength: 1000 } }, required: ["department", "objective", "inputs", "constraints", "evidenceRequired"] } } },
  { type: "function", function: { name: "CHUCK_OUTCOME_LIST", description: "List Chusky's outcome packages: department-specific missions with inputs, tools, success criteria, evidence, approvals, budgets, and deliverables.", parameters: { type: "object", properties: {} } } },
  { type: "function", function: { name: "CHUCK_OUTCOME_PLAN", description: "Plan a department outcome from a named package. This creates no external side effects; it returns missing inputs, a definition of done, and a typed step plan.", parameters: { type: "object", properties: { slug: { type: "string" }, input: { type: "object" } }, required: ["slug", "input"] } } },
  { type: "function", function: { name: "CHUCK_DAYTONA_WORKSPACE", description: "Create or inspect Chusky's private isolated Daytona computer workspace.", parameters: { type: "object", properties: { action: { type: "string", enum: ["get", "create", "status", "pause", "archive"] } }, required: ["action"] } } },
  { type: "function", function: { name: "CHUCK_DAYTONA_SANDBOX", description: "Inspect and operate an owned Daytona sandbox or a copy-on-write fork. Use health before expensive work, fork for isolated sub-agents or risky experiments, paths to discover the provider's directories, lifecycle for bounded cleanup policy, and resize only when the current resource allocation is insufficient. sandboxId may only be a primary workspace or fork created for this account.", parameters: { type: "object", properties: { action: { type: "string", enum: ["status", "health", "metrics", "paths", "fork", "start", "stop", "pause", "resize", "lifecycle", "wait_started", "wait_stopped"] }, sandboxId: { type: "string" }, name: { type: "string" }, cpu: { type: "number" }, memory: { type: "number" }, disk: { type: "number" }, timeoutSeconds: { type: "number" }, autoStopMinutes: { type: "number" }, autoPauseMinutes: { type: "number" }, ttlMinutes: { type: "number" }, autoArchiveMinutes: { type: "number" }, autoDeleteMinutes: { type: "number" } }, required: ["action"] } } },
  { type: "function", function: { name: "CHUCK_DAYTONA_VOLUME", description: "Create and manage account-owned Daytona persistent volumes. Volumes can be mounted only when a new sandbox is created; mounting or unmounting an existing sandbox is rejected safely. Delete is approval-gated.", parameters: { type: "object", properties: { action: { type: "string", enum: ["list", "create", "mount", "unmount", "delete"] }, name: { type: "string" }, mountPath: { type: "string" }, subpath: { type: "string" } }, required: ["action"] } } },
  { type: "function", function: { name: "CHUCK_DAYTONA_EXECUTE", description: "Run a bounded command of up to 64,000 characters in the user's isolated Daytona workspace. Use for short code, analysis, and file operations. Prefer a workspace-relative cwd such as workspace or workspace/project; the common /home/user/... form is normalized automatically. For servers, watchers, installs, or work that may exceed the timeout, use CHUCK_DAYTONA_PTY instead; always inspect exitCode and timedOut.", parameters: { type: "object", properties: { command: { type: "string" }, cwd: { type: "string" }, timeoutSeconds: { type: "number", description: "Maximum runtime in seconds, from 1 to 900; default 60. Use PTY for longer work." }, purpose: { type: "string" } }, required: ["command", "purpose"] } } },
  { type: "function", function: { name: "CHUCK_DAYTONA_LIST_FILES", description: "List files in the user's isolated Daytona workspace.", parameters: { type: "object", properties: { path: { type: "string" }, depth: { type: "number" } } } } },
  { type: "function", function: { name: "CHUCK_DAYTONA_READ_FILE", description: "Read a bounded text file from the user's isolated Daytona workspace. Use only a workspace-relative path returned by creation, list, find, or download results; if it is unknown, first use CHUCK_DAYTONA_LIST_FILES or CHUCK_DAYTONA_FIND_FILES. This tool never creates a missing file. Do not use it for PDFs, DOCX, PPTX, XLSX, images, audio, video, or ZIP files; use CHUCK_ARTIFACT for deliverables or the Daytona computer for visual inspection.", parameters: { type: "object", properties: { path: { type: "string" }, maxChars: { type: "number" } }, required: ["path"] } } },
  { type: "function", function: { name: "CHUCK_DAYTONA_WRITE_FILE", description: "Write text to a file in the user's isolated Daytona workspace, preferably with a workspace-relative path such as workspace/resume.html. Daytona's common /home/user/resume.html form is normalized automatically.", parameters: { type: "object", properties: { path: { type: "string" }, content: { type: "string" } }, required: ["path", "content"] } } },
  { type: "function", function: { name: "CHUCK_DAYTONA_REPLACE_FILES", description: "Replace a bounded literal string across owned Daytona workspace files using the provider's file API. Use this for small coordinated edits; inspect the resulting files and run verification afterward.", parameters: { type: "object", properties: { files: { type: "array", items: { type: "string" }, minItems: 1, maxItems: 100 }, pattern: { type: "string", maxLength: 8000 }, newValue: { type: "string", maxLength: 8000 } }, required: ["files", "pattern", "newValue"] } } },
  { type: "function", function: { name: "CHUCK_DAYTONA_SET_FILE_PERMISSIONS", description: "Change permissions or ownership on an owned Daytona workspace path. This is security-sensitive and requires approval; use the narrowest mode or owner change needed.", parameters: { type: "object", properties: { path: { type: "string" }, permissions: { type: "object", properties: { mode: { type: "string" }, owner: { type: "string" }, group: { type: "string" } } } }, required: ["path", "permissions"] } } },
  { type: "function", function: { name: "CHUCK_DAYTONA_FIND_FILES", description: "Find text matches in files in the user's isolated Daytona workspace.", parameters: { type: "object", properties: { path: { type: "string" }, pattern: { type: "string" } }, required: ["pattern"] } } },
  { type: "function", function: { name: "CHUCK_DAYTONA_SEARCH_FILES", description: "Search for files by name pattern in the user's isolated Daytona workspace.", parameters: { type: "object", properties: { path: { type: "string" }, pattern: { type: "string" } }, required: ["pattern"] } } },
  { type: "function", function: { name: "CHUCK_DAYTONA_FILE_DETAILS", description: "Get metadata for a file or directory in the user's isolated Daytona workspace. Use only a workspace-relative path returned by a creation, list, find, or download result. If the path is not known, first use CHUCK_DAYTONA_LIST_FILES or CHUCK_DAYTONA_FIND_FILES; this tool never creates a missing input file.", parameters: { type: "object", properties: { path: { type: "string" } }, required: ["path"] } } },
  { type: "function", function: { name: "CHUCK_DAYTONA_CREATE_FOLDER", description: "Create a folder in the user's isolated Daytona workspace.", parameters: { type: "object", properties: { path: { type: "string" } }, required: ["path"] } } },
  { type: "function", function: { name: "CHUCK_DAYTONA_MOVE_FILES", description: "Move or rename a file or folder in the user's isolated Daytona workspace.", parameters: { type: "object", properties: { source: { type: "string" }, destination: { type: "string" } }, required: ["source", "destination"] } } },
  { type: "function", function: { name: "CHUCK_DAYTONA_DELETE_FILE", description: "Delete a file or folder from the user's isolated Daytona workspace. Requires confirmation.", parameters: { type: "object", properties: { path: { type: "string" }, recursive: { type: "boolean" } }, required: ["path"] } } },
  { type: "function", function: { name: "CHUCK_DAYTONA_DELETE_WORKSPACE", description: "Permanently delete the user's Daytona workspace and all of its retained state. Requires confirmation.", parameters: { type: "object", properties: {} } } },
  { type: "function", function: { name: "CHUCK_DAYTONA_PREVIEW", description: "Get a signed, temporary HTTPS preview URL for a web service running on a Daytona workspace port. Include the returned URL and expiry in the final user-facing response.", parameters: { type: "object", properties: { port: { type: "number" } }, required: ["port"] } } },
  { type: "function", function: { name: "CHUCK_DAYTONA_APP", description: "Operate a durable app project in the isolated Daytona workspace. scaffold creates a Vite React TypeScript or Next.js project in workspace/apps/<id>, initializes an isolated local feature branch, and records it. verify runs typecheck, lint, tests when configured, and a required production build; start always re-runs that verification before launching a durable server and returning a signed preview URL. logs reads only the app's server PTY. visual opens the signed preview and captures a screenshot for model review; review records the model's honest visual assessment. release is only a readiness handoff after executable and visual checks pass—it never deploys or pushes. GitHub push and permanent deployment use their separate approval-gated flows.", parameters: { type: "object", properties: { action: { type: "string", enum: ["scaffold", "verify", "start", "status", "logs", "visual", "review", "branch", "release", "stop", "list"] }, id: { type: "string", description: "Lowercase app identifier, such as client-portal" }, framework: { type: "string", enum: ["vite-react", "nextjs"] }, port: { type: "number" }, expiresInSeconds: { type: "number", description: "Signed preview lifetime, 60-86400 seconds; default 3600" }, branch: { type: "string", description: "Safe local feature branch name; no remote push occurs" }, passed: { type: "boolean", description: "For review only: whether the model actually found the captured visual acceptable" }, summary: { type: "string", description: "For review or release: concise evidence or requested external target" }, target: { type: "string", description: "For release: intended approved deployment target, for example Vercel preview" } }, required: ["action"] } } },
  { type: "function", function: { name: "CHUCK_DAYTONA_CREATE_SNAPSHOT", description: "Create a reusable Daytona snapshot of the user's workspace. Requires confirmation.", parameters: { type: "object", properties: { name: { type: "string" } }, required: ["name"] } } },
  { type: "function", function: { name: "CHUCK_DAYTONA_COMPUTER", description: "Operate the user's persistent Daytona desktop and browser. Use accessibility inspection before coordinate actions. Supports lifecycle, display geometry, compressed or full-resolution screenshots, diagnostics, screen recordings, mouse, keyboard, and accessibility actions. Process diagnostics are only for Daytona desktop infrastructure: novnc (browser handoff), x11vnc, xfce4, or xvfb—never guess browser or chrome. process_status defaults to novnc when no process is supplied.", parameters: { type: "object", properties: { action: { type: "string", enum: ["start", "stop", "status", "display", "display_info", "windows", "process_status", "process_restart", "process_logs", "process_errors", "screenshot", "screenshot_full", "screenshot_region", "screenshot_region_full", "recording_start", "recording_stop", "recording_list", "recording_get", "recording_delete", "recording_download", "mouse_position", "mouse_move", "mouse_click", "mouse_drag", "mouse_scroll", "keyboard_type", "keyboard_press", "keyboard_hotkey", "accessibility_tree", "accessibility_find", "accessibility_focus", "accessibility_invoke", "accessibility_set_value"] }, x: { type: "number" }, y: { type: "number" }, width: { type: "number" }, height: { type: "number" }, startX: { type: "number" }, startY: { type: "number" }, endX: { type: "number" }, endY: { type: "number" }, button: { type: "string", enum: ["left", "right", "middle"] }, double: { type: "boolean" }, direction: { type: "string", enum: ["up", "down"] }, amount: { type: "number" }, text: { type: "string" }, delayMs: { type: "number" }, key: { type: "string" }, modifiers: { type: "array", items: { type: "string" } }, keys: { type: "string" }, showCursor: { type: "boolean" }, quality: { type: "number" }, scale: { type: "number" }, scope: { type: "string", enum: ["focused", "pid", "all"] }, pid: { type: "number" }, states: { type: "array", items: { type: "string" }, maxItems: 20 }, maxDepth: { type: "number" }, role: { type: "string" }, name: { type: "string" }, nameMatch: { type: "string", enum: ["exact", "substring", "regex"] }, limit: { type: "number" }, nodeId: { type: "string" }, nodeAction: { type: "string" }, value: { type: "string" }, processName: { type: "string", enum: ["novnc", "x11vnc", "xfce4", "xvfb", "browser", "chrome", "desktop", "vnc"] }, recordingId: { type: "string" }, label: { type: "string" }, path: { type: "string" } }, required: ["action"] } } },
  { type: "function", function: { name: "CHUCK_DAYTONA_PAUSE", description: "Pause the user's Daytona workspace while preserving its filesystem state.", parameters: { type: "object", properties: {} } } },
  { type: "function", function: { name: "CHUCK_DAYTONA_PTY", description: "Create and control a durable interactive terminal process in the user's Daytona workspace. Use create once, then write/read/status/resize/kill with the returned session ID.", parameters: { type: "object", properties: { action: { type: "string", enum: ["create", "write", "read", "status", "resize", "kill"] }, id: { type: "string" }, input: { type: "string" }, cwd: { type: "string" }, cols: { type: "number" }, rows: { type: "number" } }, required: ["action"] } } },
  { type: "function", function: { name: "CHUCK_DAYTONA_SESSION", description: "Manage a durable, stateful Daytona process session. Use it for long-running servers, background jobs, interactive commands, and resumable logs instead of repeatedly starting new shell commands. stream_logs uses Daytona's native stdout/stderr callbacks for low-latency progress. Session IDs are account-scoped and bounded by Chusky.", parameters: { type: "object", properties: { action: { type: "string", enum: ["create", "list", "get", "execute", "logs", "stream_logs", "input", "delete"] }, sandboxId: { type: "string" }, id: { type: "string" }, commandId: { type: "string" }, command: { type: "string" }, input: { type: "string" }, runAsync: { type: "boolean" }, suppressInputEcho: { type: "boolean" }, timeoutSeconds: { type: "number" } }, required: ["action"] } } },
  { type: "function", function: { name: "CHUCK_DAYTONA_CODE", description: "Run Python in an isolated Daytona interpreter context with persistent state, bounded output, optional callbacks, and explicit context ownership. To execute code, pass action=run, contextId, and code. Create a context first with action=create_context; use action=list_contexts to inspect owned contexts and action=delete_context with contextId to remove one. Use this for data analysis, document/PDF inspection, charts, and multi-step transformations instead of installing ad-hoc packages through shell commands.", parameters: { type: "object", properties: { action: { type: "string", enum: ["create_context", "list_contexts", "run", "delete_context"] }, sandboxId: { type: "string" }, contextId: { type: "string" }, cwd: { type: "string" }, code: { type: "string" }, envs: { type: "object" }, timeoutSeconds: { type: "number" } }, required: ["action"] } } },
  { type: "function", function: { name: "CHUCK_DAYTONA_LSP", description: "Use Daytona's language server for bounded code intelligence in an owned workspace: document symbols, sandbox-wide symbol search, and completions. This is read-only and helps sub-agents understand code without dumping entire repositories into context.", parameters: { type: "object", properties: { action: { type: "string", enum: ["document_symbols", "sandbox_symbols", "completions"] }, sandboxId: { type: "string" }, language: { type: "string", enum: ["typescript", "javascript", "python"] }, projectPath: { type: "string" }, path: { type: "string" }, query: { type: "string" }, line: { type: "number" }, character: { type: "number" } }, required: ["action"] } } },
  { type: "function", function: { name: "CHUCK_DAYTONA_GIT", description: "Use Daytona's Git API for a repository in the isolated workspace. Clone, inspect, branch, pull, stage, commit, and push. Push is externally visible and requires approval.", parameters: { type: "object", properties: { action: { type: "string", enum: ["clone", "status", "branches", "create_branch", "checkout", "pull", "add", "commit", "push"] }, path: { type: "string" }, repoUrl: { type: "string" }, branch: { type: "string" }, files: { type: "array", items: { type: "string" } }, message: { type: "string" }, author: { type: "string" }, email: { type: "string" }, remote: { type: "string" } }, required: ["action"] } } },
  { type: "function", function: { name: "CHUCK_DAYTONA_BROWSER", description: "Browse the web in the user's Daytona desktop through the official Computer Use API. Open http(s) URLs, inspect accessibility snapshots, find elements by role/name/scope/state, invoke or fill accessible controls, inspect windows, take compressed or full-resolution screenshots, drag and drop, manage VNC/recordings, register durable browser downloads as artifacts, wait for a download to become stable, and use keyboard navigation. Acquire a logical browser session lease before concurrent agents steer the same desktop. Navigation returns bounded accessibility evidence plus an address-bar URL observation when the desktop exposes one; consequential actions still require explicit verification. Browser state is retained in the user's Daytona workspace. During an authenticated vault session, use find before invoke/fill; coordinate clicks and keyboard submit are disabled, controls whose purpose is unclear require vaultAction=unknown and owner approval, and checkout/account changes require the matching vaultAction so Chusky can enforce identity policy.", parameters: { type: "object", properties: { action: { type: "string", enum: ["start", "stop", "status", "state", "session_acquire", "session_list", "session_release", "open", "snapshot", "find", "focus", "invoke", "fill", "windows", "display_info", "screenshot", "screenshot_full", "screenshot_region", "screenshot_region_full", "recording_start", "recording_stop", "recording_list", "recording_get", "recording_delete", "recording_download", "downloads", "wait_download", "download_register", "click", "move", "drag", "type", "press", "scroll", "back", "forward", "refresh"] }, sessionId: { type: "string" }, sessionName: { type: "string" }, missionId: { type: "string" }, ttlSeconds: { type: "number" }, timeoutSeconds: { type: "number" }, url: { type: "string" }, x: { type: "number" }, y: { type: "number" }, width: { type: "number" }, height: { type: "number" }, startX: { type: "number" }, startY: { type: "number" }, endX: { type: "number" }, endY: { type: "number" }, button: { type: "string", enum: ["left", "right", "middle"] }, text: { type: "string" }, value: { type: "string" }, key: { type: "string" }, keys: { type: "string" }, direction: { type: "string", enum: ["up", "down"] }, amount: { type: "number" }, quality: { type: "number" }, scale: { type: "number" }, scope: { type: "string", enum: ["focused", "pid", "all"] }, pid: { type: "number" }, states: { type: "array", items: { type: "string" }, maxItems: 20 }, maxDepth: { type: "number" }, role: { type: "string" }, name: { type: "string" }, nameMatch: { type: "string", enum: ["exact", "substring", "regex"] }, limit: { type: "number" }, nodeId: { type: "string" }, nodeAction: { type: "string" }, processName: { type: "string" }, recordingId: { type: "string" }, label: { type: "string" }, path: { type: "string" }, type: { type: "string", enum: ["website", "report", "docx", "presentation", "pdf", "spreadsheet", "image", "video", "zip", "project"] }, contentType: { type: "string" }, vaultAction: { type: "string", enum: ["browse", "search", "add_to_cart", "add_to_wishlist", "checkout", "place_order", "purchase", "change_address", "add_payment_method", "download_sensitive", "unknown"] } }, required: ["action"] } } },
  { type: "function", function: { name: "CHUCK_VAULT_SAVE", description: "Use this when the user asks to connect, link, add, save, sign in to, or log in to an ordinary website account that is not being connected through an explicitly supported Composio app. Start a private one-time setup for that website login. This tool never accepts a username, password, cookie, code, or secret. It returns a private HTTPS form where the user enters credentials directly into Chusky's trusted server; the model cannot read those values. Use accountAlias such as personal or work when the user has multiple identities for the same site.", parameters: { type: "object", properties: { service: { type: "string" }, origin: { type: "string", description: "Exact HTTPS origin, e.g. https://www.amazon.com" }, accountAlias: { type: "string", description: "Optional owner-defined identity alias, such as personal or work" }, loginUrl: { type: "string" }, logoutUrl: { type: "string" }, usernameFieldLabel: { type: "string", description: "Exact accessibility label for the login identifier field" }, passwordFieldLabel: { type: "string", description: "Exact accessibility label for the password field" }, submitButtonLabel: { type: "string", description: "Exact accessibility label for the sign-in button" } }, required: ["service", "origin"] } } },
  { type: "function", function: { name: "CHUCK_VAULT_LIST", description: "List only the caller's connected websites and safe session metadata. It never returns usernames, passwords, cookies, encrypted values, or keys.", parameters: { type: "object", properties: {} } } },
  { type: "function", function: { name: "CHUCK_VAULT_STATUS", description: "Check safe retained-Daytona authentication session metadata for one connected website or all of the caller's websites. It never returns credentials or cookies. Use accountAlias and origin to distinguish identities when the same service name has multiple saved websites.", parameters: { type: "object", properties: { service: { type: "string" }, accountAlias: { type: "string" }, origin: { type: "string", description: "Optional exact clean HTTPS origin" } } } } },
  { type: "function", function: { name: "CHUCK_VAULT_LOGIN", description: "Use a saved website identity through the trusted credential broker. It decrypts and fills credentials only inside Daytona, then returns only authenticated status and safe session metadata. Never attempt to type a password with any browser tool. Use accountAlias and the exact origin when multiple identities could match.", parameters: { type: "object", properties: { service: { type: "string" }, accountAlias: { type: "string" }, origin: { type: "string", description: "Optional exact clean HTTPS origin; required when service and alias match more than one saved website" } }, required: ["service"] } } },
  { type: "function", function: { name: "CHUCK_VAULT_LOGOUT", description: "Stop reusing Chusky's saved authenticated browser session for one website identity. This preserves the encrypted login until the user explicitly replaces or deletes it. Use accountAlias and origin to target one identity, or omit them to log out all matching identities.", parameters: { type: "object", properties: { service: { type: "string" }, accountAlias: { type: "string" }, origin: { type: "string", description: "Optional exact clean HTTPS origin" } }, required: ["service"] } } },
  { type: "function", function: { name: "CHUCK_SHOPPING_START", description: "Start a private, durable shopping plan for physical goods. Use when the user asks to buy, order, restock, find, compare, or add products to a cart. It suggests relevant retailers but never treats Amazon as the only option and never accepts credentials or payment details.", parameters: { type: "object", properties: { items: { type: "array", items: { type: "string" }, description: "1-80 products or shopping-list items" }, category: { type: "string", enum: ["groceries", "household", "meal_kit", "fashion", "electronics", "health_beauty", "home", "other"] }, retailer: { type: "string", description: "Optional retailer the user already chose" }, country: { type: "string", description: "Two-letter delivery country code, such as US or GB" }, city: { type: "string" }, postcode: { type: "string" }, budget: { type: "number", description: "Maximum basket budget, excluding unknown taxes or delivery fees" }, currency: { type: "string", description: "Three-letter currency code, such as USD or GBP" }, deliveryPreference: { type: "string", enum: ["delivery", "pickup", "either"] }, notes: { type: "string", description: "Dietary needs, preferred brands, substitutions, sizes, or other user-provided shopping constraints" } }, required: ["items"] } } },
  { type: "function", function: { name: "CHUCK_SHOPPING_LIST", description: "List the caller's recent private shopping plans, their selected retailer, and their safe workflow status. It never returns website credentials, cookies, payment data, or delivery addresses.", parameters: { type: "object", properties: { limit: { type: "number", minimum: 1, maximum: 50 } } } } },
  { type: "function", function: { name: "CHUCK_SHOPPING_SELECT_RETAILER", description: "Choose the retailer for a private shopping plan. Use a suggested retailer name or, for any other retailer, supply its clean HTTPS origin. Then continue with the vault/browser flow; never ask for credentials in chat.", parameters: { type: "object", properties: { id: { type: "string" }, retailer: { type: "string" }, origin: { type: "string", description: "Required only for a retailer outside Chusky's starter catalogue; clean HTTPS origin, such as https://shop.example.com" } }, required: ["id", "retailer"] } } },
  { type: "function", function: { name: "CHUCK_SHOPPING_UPDATE", description: "Update a private active shopping plan when the user changes their list, budget, location, delivery preference, or shopping constraints. It does not change a website cart by itself.", parameters: { type: "object", properties: { id: { type: "string" }, items: { type: "array", items: { type: "string" } }, category: { type: "string", enum: ["groceries", "household", "meal_kit", "fashion", "electronics", "health_beauty", "home", "other"] }, country: { type: "string" }, city: { type: "string" }, postcode: { type: "string" }, budget: { type: "number" }, currency: { type: "string" }, deliveryPreference: { type: "string", enum: ["delivery", "pickup", "either"] }, notes: { type: "string" } }, required: ["id"] } } },
  { type: "function", function: { name: "CHUCK_SHOPPING_CANCEL", description: "Cancel one private shopping plan. This stops Chusky's plan only; it never removes an external website cart, account, or order.", parameters: { type: "object", properties: { id: { type: "string" } }, required: ["id"] } } },
  { type: "function", function: { name: "CHUCK_ARTIFACT", description: "Create and manage durable user-owned deliverables in Daytona. Create text artifacts, register files generated in the workspace, list/get/delete artifacts, package workspace files into a ZIP, or request a short-lived signed download URL for a user-owned artifact. For DOCX, use a real OOXML generator with explicit page settings, Word styles, readable wrapping, properly sized images with alt text, and non-clipping tables; for XLSX, use a real workbook generator with explicit sheet names, column widths, wrapped cells, frozen headers, formulas, and charts where useful; for PPTX, use the guarded presentation generator or a real OOXML library with layout-aware spacing. Every DOCX, PDF, PPTX, and XLSX registration performs structural validation and page-by-page rendering in Daytona before delivery; the gate fails closed when LibreOffice/Poppler cannot complete the inspection. Use the Daytona computer to inspect rendered pages for clipping, overflow, broken tables, missing images, bad page breaks, and unreadable text before registering. Generate the valid file in Daytona first, then register the exact workspace-relative path returned by the generator; never invent a filename or prepend a workspace prefix. Registration adds the type-appropriate filename extension when missing and can recover one unique basename mismatch.", parameters: { type: "object", properties: { action: { type: "string", enum: ["create", "register", "list", "get", "download_url", "delete", "package"] }, id: { type: "string" }, ttlSeconds: { type: "number", minimum: 30, maximum: 3600 }, name: { type: "string" }, type: { type: "string", enum: ["website", "report", "docx", "presentation", "pdf", "spreadsheet", "image", "video", "zip", "project"] }, path: { type: "string" }, content: { type: "string" }, contentType: { type: "string" }, files: { type: "array", items: { type: "string" } }, removeFile: { type: "boolean" } }, required: ["action"] } } },
  { type: "function", function: { name: "CHUCK_EMAIL_ARTIFACT", description: "Send one or more of your durable generated artifacts as email attachments through one exact connected Composio email action. Use this after CHUCK_CREATE_PDF, CHUCK_CREATE_DOCUMENT, CHUCK_CREATE_PRESENTATION, CHUCK_CREATE_SPREADSHEET, or CHUCK_ARTIFACT succeeds. First inspect the selected email action with COMPOSIO_GET_TOOL_SCHEMAS. Set emailTool to that exact direct action slug, put the action's normal arguments (recipient, subject, body, and any provider-specific fields) inside arguments, and put the returned artifact IDs in artifactIds. Chusky reads the provider schema and inserts the file bytes server-side; never put base64, filesystem paths, credentials, or signed URLs in arguments. Sending email is externally visible and follows the normal approval policy.", parameters: { type: "object", properties: { emailTool: { type: "string", description: "Exact connected Composio email action slug, for example GMAIL_SEND_EMAIL or an Outlook send action" }, arguments: { type: "object", description: "The exact arguments required by emailTool, excluding attachment fields; use the schema returned by COMPOSIO_GET_TOOL_SCHEMAS" }, artifactIds: { type: "array", minItems: 1, items: { type: "string" }, description: "Artifact IDs returned by a successful artifact-generation tool in this run or a previous run" } }, required: ["emailTool", "arguments", "artifactIds"] } } },
  { type: "function", function: { name: "CHUCK_DELEGATE_SUBAGENT", description: "Delegate a task to a specialized domain worker capability under a typed execution contract. Classify the objective first: Nora is for source-backed web, technical, market, competitor, and research work; Lucas is mandatory for software and Daytona engineering; Maya is for social publishing and integrations; Leo is for marketing and media; Sofia is for phone or voice calls; Dexter is for GUI or computer use; Elena is for durable task operations; Ivy is for inbox and communications triage; Quinn is for sales, revenue, pipeline, and deal follow-through; Aria is for customer onboarding, success, renewals, and churn risk; Kai is for metrics, analytics, reporting, and anomaly detection. Split mixed objectives into dependency-ordered delegations. If a mixed request reaches this tool, it is safely sequenced into durable role-scoped stages rather than failing. Workers can request one missing tool and resume only after Chusky scopes it. Use only canonical Chusky names: native tools belong in allowedTools as CHUCK_*; web search belongs in allowedComposioTools as COMPOSIO_SEARCH_WEB, never web_search. Mission lifecycle tools (CHUCK_MISSION_*) are supervisor-only: use them before or after the handoff, never include them in a specialist's allowedTools.", parameters: { type: "object", properties: { worker: { type: "string", enum: ["nora", "lucas", "maya", "leo", "sofia", "dexter", "elena", "ivy", "quinn", "aria", "kai"] }, objective: { type: "string", description: "Clear objective description" }, expectedOutput: { type: "string", description: "Expected result or output spec" }, allowedTools: { type: "array", items: { type: "string" }, description: "Optional native CHUCK_* tool names allowed for this contract. Do not place web_search or CHUCK_MISSION_* tools here." }, allowedComposioTools: { type: "array", items: { type: "string" }, description: "Exact verified Composio slugs scoped to this worker and one task. Use COMPOSIO_SEARCH_WEB for live web research." }, duration: { type: "string", enum: ["5m", "30m", "1h", "3h", "6h", "3d", "1w"], description: "Overall durable worker budget; defaults to 30m" }, timeoutSeconds: { type: "number", minimum: 5, maximum: 300 }, maxToolCalls: { type: "number", minimum: 1, maximum: 100, description: "Per-slice tool-call ceiling; the overall duration remains the safety boundary." } }, required: ["worker", "objective"] } } },
  { type: "function", function: { name: "CHUCK_HANDOFF_SUBAGENT", description: "Hand off an active task or sub-objective to another domain worker capability (nora, lucas, maya, sofia, dexter, elena, ivy, quinn, aria, kai, or leo).", parameters: { type: "object", properties: { targetWorker: { type: "string", enum: ["nora", "lucas", "maya", "leo", "sofia", "dexter", "elena", "ivy", "quinn", "aria", "kai"] }, objective: { type: "string", description: "Objective for target worker" }, expectedOutput: { type: "string" }, context: { type: "object" } }, required: ["targetWorker", "objective"] } } },
  { type: "function", function: { name: "CHUCK_PLAN_DELEGATION", description: "Classify an objective across all matching capabilities and return a deterministic dependency plan before any worker is created. Use this for mixed requests such as build an app, create its marketing assets, and publish them; execute each returned step with its assigned worker and pass only the prior step's handoff.", parameters: { type: "object", properties: { objective: { type: "string", description: "Full objective to decompose" }, allowedTools: { type: "array", items: { type: "string" }, description: "Optional native tools that further clarify the capability" } }, required: ["objective"] } } },
  { type: "function", function: { name: "CHUCK_REQUEST_ADDITIONAL_TOOLS", description: "Worker-only escalation: request a missing capability from Chusky. Do not guess a Composio slug or search the catalogue yourself. State the user-facing intent, why the current allowed tools are insufficient, and an optional provider/toolkit. Chusky will decide whether to use COMPOSIO_SEARCH_TOOL, verify the user's connection, and resume this same paused worker with one narrowly scoped action. This tool never grants or executes a capability itself.", parameters: { type: "object", properties: { intent: { type: "string", description: "What action is needed, expressed as an intent rather than a guessed tool slug" }, reason: { type: "string", description: "Why the worker's current tools cannot complete the task" }, preferredToolkit: { type: "string", description: "Optional likely toolkit, such as github or slack; omit when uncertain" } }, required: ["intent", "reason"] } } },
  { type: "function", function: { name: "CHUCK_RESOLVE_SUBAGENT_TOOL_REQUEST", description: "Supervisor-only durable continuation. After COMPOSIO_SEARCH_TOOL has identified and Chusky has verified exact connected action slugs, notify the specific worker workflow waiting for a tool decision. This resumes the same durable worker task; it never grants a broad toolkit or runs the external action itself.", parameters: { type: "object", properties: { handoffId: { type: "string" }, allowedComposioTools: { type: "array", minItems: 1, items: { type: "string" }, description: "Exact verified Composio action slugs, scoped to the waiting worker's role" } }, required: ["handoffId", "allowedComposioTools"] } } },
  { type: "function", function: { name: "CHUCK_REVIEW_SUBAGENT_ACTION", description: "Supervisor-only denial of a specialist risky-action proposal. The supervisor cannot approve its own proposal; the account owner must use the explicit approval controls. Denial stops the exact pending action.", parameters: { type: "object", properties: { approvalId: { type: "string" }, decision: { type: "string", enum: ["deny"] } }, required: ["approvalId", "decision"] } } },
  { type: "function", function: { name: "CHUCK_LIST_SUBAGENTS", description: "List recent subagent delegation handoff records. Use to answer 'What is Lucas doing?', 'Which agent failed?', or 'Show all active workers.'", parameters: { type: "object", properties: { limit: { type: "number", minimum: 1, maximum: 50, description: "Max records to return; defaults to 20" } } } } },
  { type: "function", function: { name: "CHUCK_GET_SUBAGENT_STATUS", description: "Get full status, tool call log, and linked task details for a specific subagent delegation by handoff record ID.", parameters: { type: "object", properties: { id: { type: "string", description: "The handoff record ID" } }, required: ["id"] } } },
  { type: "function", function: { name: "CHUCK_CANCEL_SUBAGENT", description: "Cancel a worker delegation and its linked durable task by handoff record ID. Use when the user says 'cancel Dexter's task' or 'stop the browser agent'. Cannot interrupt mid-flight execution.", parameters: { type: "object", properties: { id: { type: "string", description: "The handoff record ID to cancel" }, reason: { type: "string", description: "Reason for cancellation" } }, required: ["id"] } } },
] as const;

/** Kept separate from the legacy catalogue so the human-browser handoff stays reviewable. */
const shoppingAndBrowserTools = [
  { type: "function", function: { name: "CHUCK_BROWSER_PLAN", description: "Create a safe, bounded browser-operations plan for a user goal. It classifies the likely action, identifies approval/stop boundaries, verification checks, and recovery or private handoff steps. It does not open a website or perform an action.", parameters: { type: "object", properties: { goal: { type: "string", maxLength: 1500 }, origin: { type: "string", description: "Optional clean HTTPS origin" }, accountAlias: { type: "string", description: "Optional saved website account alias" } }, required: ["goal"] } } },
  { type: "function", function: { name: "CHUCK_BROWSER_SESSION_HEALTH", description: "Check the caller's saved website sessions before browser work. Returns healthy, stale, expired, needs-reauth, logged-out, or unknown state with the safest next action; never returns cookies or credentials. Use origin when the service has multiple saved websites.", parameters: { type: "object", properties: { service: { type: "string" }, origin: { type: "string", description: "Optional exact clean HTTPS origin" } } } } },
  { type: "function", function: { name: "CHUCK_BROWSER_SESSION_REVOKE", description: "Revoke a saved website browser session, attempt the configured site logout, pause the Daytona workspace, and block browser reuse until a fresh vault login succeeds. Use accountAlias and origin to target one identity; this preserves the encrypted website login and user files.", parameters: { type: "object", properties: { service: { type: "string" }, accountAlias: { type: "string" }, origin: { type: "string", description: "Optional exact clean HTTPS origin" } }, required: ["service"] } } },
  { type: "function", function: { name: "CHUCK_BROWSER_PLAYBOOK_SAVE", description: "Save an owner-approved, origin-scoped browser login or task recipe. Recipes contain only accessible labels, safe detectors, and action names—never credentials, cookies, raw page content, or screenshots. Use after a verified successful flow.", parameters: { type: "object", properties: { service: { type: "string" }, origin: { type: "string" }, accountAlias: { type: "string" }, login: { type: "object" }, tasks: { type: "array", items: { type: "object" } } }, required: ["service", "origin", "login"] } } },
  { type: "function", function: { name: "CHUCK_BROWSER_PLAYBOOK_LIST", description: "List the caller's saved origin-scoped browser playbooks and safe verification metadata. It never returns credentials, cookies, screenshots, or raw page data.", parameters: { type: "object", properties: { origin: { type: "string" }, limit: { type: "number", minimum: 1, maximum: 50 } } } } },
  { type: "function", function: { name: "CHUCK_BROWSER_PLAYBOOK_REMOVE", description: "Remove one of the caller's saved browser playbooks. This does not delete the website login or external account.", parameters: { type: "object", properties: { id: { type: "string" } }, required: ["id"] } } },
  { type: "function", function: { name: "CHUCK_BROWSER_VERIFY", description: "Verify a browser operation against bounded URL, title, or page-text detectors. Chusky reads the retained live Daytona browser state itself; each detector passes when any observed URL/title/text condition matches, and all required detectors must pass. Results include per-detector evidence without returning full page content. After a private CAPTCHA/2FA handoff, pass its handoffId and at least one required detector; Daytona must observe the address-bar URL and the origin must match before the retained browser session is re-authorized. Never pass credentials, cookies, or a full private page dump.", parameters: { type: "object", properties: { handoffId: { type: "string", description: "The private browser handoff ID returned by CHUCK_DAYTONA_BROWSER_HANDOFF or CHUCK_VAULT_LOGIN" }, detectors: { type: "array", maxItems: 12, items: { type: "object", properties: { urlIncludes: { type: "string", maxLength: 300 }, titleIncludes: { type: "string", maxLength: 300 }, textIncludes: { type: "string", maxLength: 300 }, required: { type: "boolean" } }, additionalProperties: false } } }, required: ["detectors"] } } },
  { type: "function", function: { name: "CHUCK_BROWSER_AUDIT_LIST", description: "Show the caller's bounded, private browser activity log: site, safe action class, outcome, verification, and handoff states. It never includes credentials, cookies, raw page content, or full provider payloads.", parameters: { type: "object", properties: { limit: { type: "number", minimum: 1, maximum: 100 } } } } },
  { type: "function", function: { name: "CHUCK_BROWSER_HANDOFF_STATUS", description: "Inspect the caller's private browser handoff state without exposing the signed URL, cookies, challenge contents, or credentials. Use an id for one handoff or omit it for recent handoffs.", parameters: { type: "object", properties: { id: { type: "string" }, limit: { type: "number", minimum: 1, maximum: 20 } } } } },
  { type: "function", function: { name: "CHUCK_BROWSER_HANDOFF_COMPLETE", description: "Acknowledge that the owner has finished the private CAPTCHA/2FA/browser step. This only moves the handoff to awaiting verification; inspect the retained same-origin page and call CHUCK_BROWSER_VERIFY with the handoffId before invoking or filling anything.", parameters: { type: "object", properties: { id: { type: "string" } }, required: ["id"] } } },
  { type: "function", function: { name: "CHUCK_DAYTONA_BROWSER_HANDOFF", description: "Create a short-lived private link to the caller's existing Daytona browser desktop when they must personally complete CAPTCHA, 2FA, consent, or another website-only challenge. It preserves the same browser session for Chusky to resume afterward. Never use in a shared group and never include passwords, cookies, codes, or any secret.", parameters: { type: "object", properties: { shoppingPlanId: { type: "string", description: "Optional private shopping plan to mark as paused while the user completes the website step" }, service: { type: "string", description: "Optional saved website service label" }, origin: { type: "string", description: "Optional exact clean HTTPS origin" }, reason: { type: "string", enum: ["captcha", "two_factor", "age_verification", "site_challenge", "login", "user_requested"] } } } } },
  { type: "function", function: { name: "CHUCK_SHOPPING_PAUSE", description: "Pause a private shopping plan when a website requires CAPTCHA, 2FA, age verification, a login step, or another user-only action. It never changes a website cart or account.", parameters: { type: "object", properties: { id: { type: "string" }, reason: { type: "string", enum: ["captcha", "two_factor", "age_verification", "site_challenge", "login", "user_requested"] } }, required: ["id", "reason"] } } },
  { type: "function", function: { name: "CHUCK_SHOPPING_RESUME", description: "Resume one of the caller's paused private shopping plans after they finish a user-only website step in the retained browser. Inspect the page before taking any new action.", parameters: { type: "object", properties: { id: { type: "string" } }, required: ["id"] } } },
  { type: "function", function: { name: "CHUCK_SHOPPING_SAVE_SITE", description: "Save a user-owned shopping-site preference by name and clean HTTPS origin. This saves no password, cookie, payment data, or website session. The site can then be selected by name in later shopping plans.", parameters: { type: "object", properties: { name: { type: "string" }, origin: { type: "string" }, countries: { type: "array", items: { type: "string" } }, categories: { type: "array", items: { type: "string" } }, supportsDelivery: { type: "boolean" }, supportsPickup: { type: "boolean" } }, required: ["name", "origin"] } } },
  { type: "function", function: { name: "CHUCK_SHOPPING_LIST_SITES", description: "List only the caller's saved shopping-site names and safe metadata. It never returns credentials, cookies, payment details, or browser sessions.", parameters: { type: "object", properties: { limit: { type: "number", minimum: 1, maximum: 100 } } } } },
  { type: "function", function: { name: "CHUCK_SHOPPING_REMOVE_SITE", description: "Remove one of the caller's saved shopping-site preferences. It does not delete a separately saved vault login or any external account.", parameters: { type: "object", properties: { id: { type: "string" } }, required: ["id"] } } },
] as const;

const toolReliabilityTools = [
  { type: "function", function: { name: "CHUCK_TOOL_PREFLIGHT", description: "Check whether one exact tool is exposed in this run, validate its proposed arguments against the exact schema shown to the model, and report whether normal human approval is required. This is inspection only: it never searches for, grants, approves, or executes a tool. Use before a complex or high-impact call when argument correctness is uncertain.", parameters: { type: "object", properties: { toolName: { type: "string", minLength: 1, maxLength: 200 }, arguments: { type: "object", additionalProperties: true } }, required: ["toolName", "arguments"], additionalProperties: false } } },
  { type: "function", function: { name: "CHUCK_INTEGRATION_HEALTH", description: "Inspect owner-scoped Composio connection status. This reports only the provider's account status; it does not test each app scope or perform a live write. Unknown statuses remain unverified. Never returns credentials or raw provider payloads.", parameters: { type: "object", properties: { toolkit: { type: "string", maxLength: 120, description: "Optional toolkit slug such as gmail or slack" } }, additionalProperties: false } } },
  { type: "function", function: { name: "CHUCK_ARTIFACT_QA", description: "Independently validate and render-check a workspace-relative PDF, DOCX, PPTX, or XLSX without registering, rewriting, or delivering it. Returns bounded structural and rendered-page evidence; isolated renderer capacity may be needed, and QA fails closed if it is unavailable. It cannot certify editorial correctness or accessibility. Use after generation and before CHUCK_ARTIFACT registration when a file needs an explicit QA pass.", parameters: { type: "object", properties: { path: { type: "string", minLength: 1, maxLength: 1000 }, type: { type: "string", enum: ["pdf", "docx", "presentation", "spreadsheet"] }, expectedTitle: { type: "string", maxLength: 300 }, expectedFormulaValues: { type: "object", maxProperties: 100, additionalProperties: { type: ["string", "number", "boolean"] }, description: "Optional independent assertions keyed as sheet1.xml!C4 for spreadsheet formulas." } }, required: ["path", "type"], additionalProperties: false } } },
  { type: "function", function: { name: "CHUCK_FILE_BRIDGE", description: "Upload one of your owner-owned registered Daytona artifacts to one exact connected Composio app action. First inspect the exact action schema; supply its normal non-file arguments and artifactId. Chusky injects file bytes server-side only when the current session schema unambiguously declares a base64/binary field. The external upload requires approval, never accepts a local path, URL, credential, or model-supplied file bytes, and never downloads arbitrary provider URLs. Optional account selects an owner-connected account ID or alias.", parameters: { type: "object", properties: { artifactId: { type: "string", minLength: 1, maxLength: 160 }, toolSlug: { type: "string", minLength: 1, maxLength: 200 }, arguments: { type: "object", additionalProperties: true }, account: { type: "string", maxLength: 160 } }, required: ["artifactId", "toolSlug", "arguments"], additionalProperties: false } } },
  { type: "function", function: { name: "CHUCK_MEDIA_BRIDGE", description: "Transfer one current, generated, or saved owner image through an exact connected Composio action when directly requested; no separate approval step. First discover the exact provider action and its schema, then provide its normal non-media arguments. For one-step actions, Chusky uses only schema-declared Composio file_uploadable fields, explicit media URL fields, or unambiguous binary fields; this can cover supported social, mail, and other Composio actions without hard-coded field guesses. Instagram image posts create the media container and publish its returned ID; X image posts upload media and publish only after receiving the uploaded media ID; LinkedIn image posts initialize, upload to the returned HTTPS URL, then publish its returned image URN. Never accept model-supplied bytes, local paths, arbitrary URLs, or credentials. Unknown or ambiguous multi-step APIs fail closed. Report success only from the final provider action's confirmed success receipt.", parameters: { type: "object", properties: { source: { type: "string", enum: ["current", "generated", "asset"], description: "Where the image comes from: current inbound image, generated image from this run, or a previously saved image asset." }, sourceIndex: { type: "number", minimum: 0, maximum: 9, description: "Zero-based image index for current or generated images; defaults to 0." }, assetId: { type: "string", maxLength: 160, description: "Saved image asset ID or exact name when source=asset." }, toolSlug: { type: "string", minLength: 1, maxLength: 200, description: "Exact connected Composio action slug." }, arguments: { type: "object", additionalProperties: true, description: "The action's normal arguments excluding its image/media field." }, account: { type: "string", maxLength: 160, description: "Optional owner-connected account alias or ID." } }, required: ["source", "toolSlug", "arguments"], additionalProperties: false } } },
  { type: "function", function: { name: "CHUCK_TOOL_RECOVERY", description: "Inspect a persisted tool result from your own agent run and recommend whether it already succeeded, is safe to correct/retry because execution never began, or requires provider-state verification first. This tool never replays an action and never returns raw arguments, tool output, or error text. Omit runId to inspect the current run; an optional toolCallId selects one call.", parameters: { type: "object", properties: { runId: { type: "string", maxLength: 200 }, toolCallId: { type: "string", maxLength: 200 } }, additionalProperties: false } } },
] as const;

export const chuckTools = [...baseChuckTools, ...shoppingAndBrowserTools, ...toolReliabilityTools] as const;

// Formula cells are explicit, bounded per-sheet inputs. The Daytona boundary
// validates them again and LibreOffice independently recalculates them before
// the artifact is registered.
const spreadsheetToolSchema = chuckTools.find((item) => item.function.name === "CHUCK_CREATE_SPREADSHEET") as any;
spreadsheetToolSchema.function.description += " Formula cells may be supplied per sheet as formulas [{cell,formula,expectedValue?}]. Only basic arithmetic and the documented safe functions are accepted; expectedValue is checked against a fresh LibreOffice calculation. Returned verification evidence reports extracted content and formula counts; it is not a substitute for reviewing business assumptions.";
spreadsheetToolSchema.function.parameters.properties.sheets.items.properties = {
  name: { type: "string" },
  rows: { description: "Cell matrix or {headers,rows}; use numeric/boolean cells for calculations. Formula cells are applied separately after the table is written." },
  formulas: { type: "array", items: { type: "object", properties: { cell: { type: "string", description: "Cell address such as C4" }, formula: { type: "string", description: "Safe formula such as SUM(B4:B8), optionally with a leading =" }, expectedValue: { type: ["string", "number", "boolean"], description: "Optional expected result checked independently after recalculation" } }, required: ["cell", "formula"] } },
  tabColor: { type: "string", description: "Optional six-digit hex color" },
};
const documentToolSchema = chuckTools.find((item) => item.function.name === "CHUCK_CREATE_DOCUMENT") as any;
documentToolSchema.function.description += " Before registration, Chusky independently extracts the rendered text, checks that it contains the requested title, and renders every page; returned evidence gives page and extracted-text counts, not human editorial approval.";
const pdfToolSchema = chuckTools.find((item) => item.function.name === "CHUCK_CREATE_PDF") as any;
pdfToolSchema.function.description += " Before registration, Chusky independently extracts the rendered text, checks that it contains the requested title, and renders every page; returned evidence gives page and extracted-text counts, not human editorial approval.";

/** Validate local-tool arguments at the execution boundary, not just in the model prompt. */
export function validateNativeToolArguments(name: string, args: Record<string, unknown>): void {
  const tool = chuckTools.find((item) => item.function.name === name);
  if (!tool) throw new Error(`Unknown native tool: ${name}`);
  if (!args || typeof args !== "object" || Array.isArray(args)) throw new Error(`${name} arguments must be an object`);
  const schema = tool.function.parameters as JsonSchema;
  // A Python payload plus an existing interpreter context unambiguously means
  // "run". Keep create/list/delete explicit because they have different effects.
  if (name === "CHUCK_DAYTONA_CODE" && args.action === undefined && typeof args.code === "string" && args.code.trim() && typeof args.contextId === "string" && args.contextId.trim()) {
    args.action = "run";
  }
  // Models occasionally serialize structured arguments as JSON text, or emit
  // one PDF section instead of a one-item array. Normalize only these safe
  // structural forms before enforcing the schema so a recoverable formatting
  // mistake does not abort an otherwise valid document request.
  for (const [key, rule] of Object.entries(schema.properties ?? {})) {
    if (rule.type !== "array") continue;
    const value = args[key];
    if (typeof value === "string") {
      try {
        const parsed = JSON.parse(value);
        if (Array.isArray(parsed)) args[key] = parsed;
      } catch {
        // The normal type error below remains actionable for non-JSON text.
      }
    } else if (name === "CHUCK_CREATE_PDF" && key === "sections" && value && typeof value === "object" && !Array.isArray(value)) {
      args[key] = [value];
    }
  }
  validateToolArgumentsAgainstSchema(name, args, schema);
}

/** Validate provider-generated arguments against the exact advertised schema. */
export function validateToolArgumentsAgainstSchema(name: string, args: Record<string, unknown>, inputSchema: unknown, maxBytes = 1_048_576): void {
  if (!args || typeof args !== "object" || Array.isArray(args)) throw new Error(`${name} arguments must be an object`);
  let serialized: string;
  try { serialized = JSON.stringify(args); } catch { throw new Error(`${name} arguments are not valid JSON data`); }
  if (!Number.isSafeInteger(maxBytes) || maxBytes < 1 || maxBytes > 40 * 1024 * 1024) throw new Error("Invalid internal argument validation limit");
  if (Buffer.byteLength(serialized, "utf8") > maxBytes) throw new Error(`${name} arguments exceed the ${maxBytes}-byte limit`);
  if (!inputSchema || typeof inputSchema !== "object" || Array.isArray(inputSchema)) throw new Error(`${name} has an invalid argument schema`);
  const errors: string[] = [];
  validateJsonSchema(args, inputSchema as JsonSchema, "", name, errors, { nodes: 0 });
  if (errors.length) throw new Error(errors[0]);
}

type JsonSchema = {
  type?: string | readonly string[];
  enum?: readonly unknown[];
  required?: readonly string[];
  properties?: Record<string, JsonSchema>;
  items?: JsonSchema;
  additionalProperties?: boolean | JsonSchema;
  minimum?: number;
  maximum?: number;
  minLength?: number;
  maxLength?: number;
  minItems?: number;
  maxItems?: number;
  minProperties?: number;
  maxProperties?: number;
  pattern?: string;
};

function jsonSchemaTypeMatches(value: unknown, type: string): boolean {
  switch (type) {
    case "null": return value === null;
    case "object": return typeof value === "object" && value !== null && !Array.isArray(value);
    case "array": return Array.isArray(value);
    case "integer": return typeof value === "number" && Number.isFinite(value) && Number.isInteger(value);
    case "number": return typeof value === "number" && Number.isFinite(value);
    case "string": return typeof value === "string";
    case "boolean": return typeof value === "boolean";
    default: return false;
  }
}

function validateJsonSchema(value: unknown, schema: JsonSchema, path: string, toolName: string, errors: string[], budget: { nodes: number }, depth = 0): void {
  budget.nodes += 1;
  if (depth > 64) throw new Error(`${toolName} arguments exceed the maximum nesting depth of 64`);
  if (budget.nodes > 25_000) throw new Error(`${toolName} arguments exceed the maximum of 25000 validated values`);
  const displayPath = path ? `${toolName}${path.startsWith("[") ? "" : "."}${path}` : toolName;
  const types = schema.type === undefined ? [] : Array.isArray(schema.type) ? schema.type : [schema.type];
  if (types.length && !types.some((type) => jsonSchemaTypeMatches(value, type))) {
    errors.push(`${displayPath} must be ${types.join(" or ")}`);
    return;
  }
  if (schema.enum && !schema.enum.some((allowed) => Object.is(allowed, value))) {
    errors.push(`${displayPath} has an unsupported value`);
    return;
  }
  if (typeof value === "number") {
    if (schema.minimum !== undefined && value < schema.minimum) errors.push(`${displayPath} must be at least ${schema.minimum}`);
    if (schema.maximum !== undefined && value > schema.maximum) errors.push(`${displayPath} must be at most ${schema.maximum}`);
  }
  if (typeof value === "string") {
    if (schema.minLength !== undefined && value.length < schema.minLength) errors.push(`${displayPath} must have at least ${schema.minLength} characters`);
    if (schema.maxLength !== undefined && value.length > schema.maxLength) errors.push(`${displayPath} exceeds maxLength ${schema.maxLength}`);
    if (schema.pattern !== undefined) {
      let matches = false;
      try { matches = new RegExp(schema.pattern).test(value); } catch { errors.push(`${toolName} has an invalid schema pattern at ${path}`); return; }
      if (!matches) errors.push(`${displayPath} does not match the required pattern`);
    }
  }
  if (Array.isArray(value)) {
    if (schema.minItems !== undefined && value.length < schema.minItems) errors.push(`${displayPath} must contain at least ${schema.minItems} items (minItems)`);
    if (schema.maxItems !== undefined && value.length > schema.maxItems) errors.push(`${displayPath} exceeds maxItems ${schema.maxItems}`);
    if (schema.items) value.forEach((item, index) => validateJsonSchema(item, schema.items!, `${path}[${index}]`, toolName, errors, budget, depth + 1));
  }
  if (value !== null && typeof value === "object" && !Array.isArray(value)) {
    const object = value as Record<string, unknown>;
    const keys = Object.keys(object);
    if (keys.length > 25_000) throw new Error(`${toolName} arguments exceed the maximum of 25000 object properties`);
    if (schema.minProperties !== undefined && keys.length < schema.minProperties) errors.push(`${displayPath} must contain at least ${schema.minProperties} properties`);
    if (schema.maxProperties !== undefined && keys.length > schema.maxProperties) errors.push(`${displayPath} exceeds maxProperties ${schema.maxProperties}`);
    for (const required of schema.required ?? []) {
      if (!(required in object) || object[required] === undefined || object[required] === null || (typeof object[required] === "string" && !object[required].trim())) {
        errors.push(`${displayPath} requires argument: ${required}`);
      }
    }
    for (const [key, child] of Object.entries(object)) {
      const childSchema = schema.properties?.[key];
      const childPath = path ? `${path}.${key}` : key;
      if (childSchema) validateJsonSchema(child, childSchema, childPath, toolName, errors, budget, depth + 1);
      else if (schema.additionalProperties === false) errors.push(`${toolName}.${childPath} is not allowed`);
      else if (schema.additionalProperties && typeof schema.additionalProperties === "object") validateJsonSchema(child, schema.additionalProperties, childPath, toolName, errors, budget, depth + 1);
    }
  }
}






